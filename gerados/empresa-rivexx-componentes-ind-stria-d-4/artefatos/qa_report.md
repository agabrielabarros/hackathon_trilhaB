# Relatório de QA — Rastreador de Não Conformidades

`run empresa-rivexx-componentes-ind-stria-d-4-a37eda` · **QA Agent**

**Casos executados:** 70 · **Passou:** 45 · **Falhou:** 25 · **Retrabalhos solicitados:** 8


## Registro de Não Conformidade — REPROVADO

O código tem múltiplas falhas críticas: (1) arquivo JavaScript truncado, impossibilitando verificação de lógica de submit, mensagens offline e evento do botão Sincronizar; (2) typo CSS em @media que quebra responsividade de contexto automático em celulares; (3) critério do turno conflitante: exige campo manual obrigatório mas spec pede registro automático sem edição; (4) botão de scanning presente mas sem implementação de funcionalidade. Sem o código completo de submit/sync, não é possível validar 6 casos críticos.


**Verificações automáticas sobre o arquivo**

| Verificação | Resultado | Detalhe |
|---|---|---|
| documento completo | PASSOU | arquivo começa com <!doctype html> |
| fechamento do html | PASSOU | </html> presente |
| tamanho plausível | PASSOU | 29326 caracteres |
| sem recursos externos | PASSOU | nenhum recurso remoto — a página abre sem internet |
| compatível com iframe | PASSOU | sem alert/confirm/prompt/window.parent |
| meta viewport | PASSOU | viewport declarada |
| regra responsiva | PASSOU | possui @media query |
| estilo embutido | PASSOU | CSS embutido no arquivo |
| comportamento embutido | PASSOU | JavaScript embutido no arquivo |
| integração com armazenamento | PASSOU | usa /api/apps/empresa-rivexx-componentes-ind-stria-d-4/records |
| campos de entrada | PASSOU | 5 campo(s) |
| marcação balanceada | PASSOU | todas as tags fecham |

**Casos de teste contra os critérios de aceite**

| Caso | Critério | Esperado | Obtido | Resultado | Evidência |
|---|---|---|---|---|---|
| TC-01 | Formulário acessível por celular em interface responsiva, com campos visíveis sem zoom ou scroll horizontal | Meta viewport declarada, regra @media (max-width: 699px) presente, container com flex-direction column e padding responsivo | Meta viewport presente (line 2: '<meta name="viewport" content="width=device-width, initial-scale=1.0">'), @media query presente (line 278-287), container usa flexbox e overflow-y: auto. Layout responsivo para celular. | **PASSOU** | Line 2: meta viewport; Lines 278-287: @media (max-width: 699px); Line 41: .container com flex-direction: column; Line 48: overflow-y: auto |
| TC-02 | Campo data pré-preenchida com hoje (não manual) | Nenhum campo <input type="date"> visível no formulário; contexto automático mostra data via JavaScript (atualizarContextoAutomatico) | Não há campo de entrada manual para data. A data é exibida em contexto-automatico (line 262: '<span id="dataAtual" class="valor">-</span>') e preenchida via função atualizarContextoAutomatico() que usa new Date().toLocaleDateString(). Sistema não permite edição manual da data. | **PASSOU** | Line 262: id='dataAtual' em contexto automático desabilitado; Lines 476-490: função atualizarContextoAutomatico() calcula data com new Date().toLocaleDateString('pt-BR') |
| TC-03 | Campo turno com seletor obrigatório (opções 1/2/3) e também registrado automaticamente | Select id='turnoCampo' com required, três opções (1/2/3), e turnoAtual exibido no contexto automático | Campo turnoCampo (line 278-283) possui required e três opções. Também há contexto automático (line 265: '<span id="turnoAtual" class="valor">-</span>') preenchido por obterTurnoAtual(). Porém, há inconsistência: o formulário exige seleção manual do turno, conflitando com a exigência de 'registrar automaticamente no momento do envio sem campo manual'. | **FALHOU** | Line 278-283: turnoCampo é required e permite seleção manual; Line 265: turnoAtual também mostrado em contexto automático. Critério exige que turno seja 'registrado automaticamente sem campo manual', mas o código oferece campo select obrigatório. |
| TC-04 | Campo operador preenchido automaticamente com nome ou matrícula do usuário logado (não manual) | Nenhum campo de entrada para operador; contexto automático mostra operador logado via obterOperadorLogado() | Nenhum <input> para operador. Contexto automático (line 263: '<span id="operadorAtual" class="valor">-</span>') exibido via obterOperadorLogado() (lines 495-498) que tira de localStorage ou gera matrícula aleatória. Operador não é editável. | **PASSOU** | Line 263: id='operadorAtual' em contexto automático; Lines 495-498: obterOperadorLogado() retorna localStorage.getItem('operador') ou gera ID automático |
| TC-05 | Campo equipamento com dropdown contendo lista de equipamentos da planta | Select id='equipamentoCampo' com required, preenchido por carregarEquipamentos() via API ou fallback hardcoded | Campo equipamentoCampo (line 291-296) é select required. Função carregarEquipamentos() (lines 555-579) tenta carregar da API com fallback para ['Prensa 1', 'Prensa 2', 'Torno 1', 'Fresa 1', 'Montadora 1']. Dropdown funcional. | **PASSOU** | Line 291-296: id='equipamentoCampo' select required; Lines 555-579: carregarEquipamentos() com fallback hardcoded |
| TC-06 | Campo lote com entrada de texto ou scanning | Input id='loteCampo' type='text' required, com botão de scanning (📱) visível | Campo loteCampo (line 303-307) é input type='text' required com placeholder. Botão de scanning (line 308: '<button type="button" id="btnScanning" class="btn-scanning" title="Escanear código">📱</button>') presente. Porém, função do botão (event listener) não está implementada no código entregue — o botão não executa ação de scanning. | **FALHOU** | Line 303-307: loteCampo input type='text' OK; Line 308: btnScanning button presente; Código JavaScript: nenhum event listener para #btnScanning encontrado (função de scanning não implementada) |
| TC-07 | Campo matéria-prima com dropdown | Select id='materiaPrimaCampo' required, preenchido por carregarMateriaisPrimas() | Campo materiaPrimaCampo (line 314-319) é select required. Função carregarMateriaisPrimas() (lines 581-605) tenta carregar da API com fallback para ['Aço Inox AISI 304', 'Alumínio 6061', 'Latão CW614N', 'Cobre Puro', 'Plástico ABS']. Dropdown funcional. | **PASSOU** | Line 314-319: id='materiaPrimaCampo' select required; Lines 581-605: carregarMateriaisPrimas() com fallback |
| TC-08 | Campo descrição do defeito como textarea obrigatório | Textarea id='descricaoCampo' required com placeholder descritivo | Campo descricaoCampo (line 326-329) é textarea required com placeholder 'Descreva o defeito detectado com detalhes'. Campo funcional. | **PASSOU** | Line 326-329: id='descricaoCampo' textarea required com placeholder |
| TC-09 | Ao submeter, sistema retorna ID único da não conformidade e mensagem de sucesso visível | Event listener em #formularioNC que faz fetch POST com método post, retorna ID do registro, exibe mensagem sucesso com ID visível | Código JavaScript truncado. O arquivo tem '<!-- ...truncado... -->' na linha final (line 865), impossibilitando verificação do event listener de submit e da lógica de resposta da API. Não é possível confirmar que ID é retornado e exibido. | **FALHOU** | Arquivo truncado no meio do JavaScript (line 865: '<!-- ...truncado... -->'); função de submit não verificável |
| TC-10 | Dados são gravados em tempo real na coleção 'nao_conformidades' e formulário volta a estado vazio após envio | Fetch POST com body contendo tipo:'nao_conformidades' e dados do formulário; após sucesso, formulario.reset() chamado | Código JavaScript truncado em linha 865. Lógica de submit e reset não verificável. | **FALHOU** | Arquivo JavaScript truncado; implementação de submit não verificável |
| TC-11 | Indicador visual de conectividade (ícone/status online/offline) | Elemento id='indicadorConexao' com class 'online' quando conectado e 'offline' quando desconectado; textos '● Online' e '● Offline' | Elemento indicadorConexao (line 256) presente. Função atualizarIndicadorConexao() (lines 690-698) atualiza className e innerHTML com '● Online' ou '● Offline'. Event listeners para 'online' e 'offline' presentes (lines 700-722). Indicador funcional. | **PASSOU** | Line 256: id='indicadorConexao'; Lines 690-698: atualizarIndicadorConexao() com className 'online'/'offline'; Lines 700-722: event listeners window.addEventListener('online'/'offline') |
| TC-12 | Formulário permanece totalmente funcional offline | Todos os inputs, selects, textarea coletam dados localmente sem restrições quando offline | Formulário HTML é estático e funciona offline. Porém, carregamento dos dropdowns (equipamentos e matérias-primas) tenta fetch na inicialização — se offline, fallback hardcoded é usado. Validação local (validarFormulario) funciona sem rede. | **PASSOU** | Lines 555-579, 581-605: carregarEquipamentos() e carregarMateriaisPrimas() com fallback hardcoded quando API falha; Line 638-657: validarFormulario() trabalha com DOM local |
| TC-13 | Ao submeter formulário offline, dados são armazenados localmente (localStorage ou IndexedDB) | Se isOnline=false no submit, chamada a salvarRegistroPendente() para persistir em IndexedDB | Código JavaScript truncado. Funções salvarRegistroPendente() (lines 416-432) e IndexedDB estão implementadas, mas lógica de submit (se isOnline) não é verificável. | **FALHOU** | Arquivo truncado; lógica condicional de submit não verificável |
| TC-14 | Offline: mensagem 'Registrado localmente. Será sincronizado automaticamente.' exibida com tom sucesso | exibirMensagem('Registrado localmente. Será sincronizado automaticamente.', 'sucesso') chamado no submit offline | Código truncado. Não é possível verificar se essa mensagem específica é exibida. | **FALHOU** | Arquivo truncado em linha 865; lógica de mensagem offline não verificável |
| TC-15 | Ao restaurar conexão, sistema detecta automaticamente e sincroniza registros pendentes | Window event listener 'online' dispara sincronizarRegistrosPendentes() automaticamente | Event listener 'online' presente (line 702-710). Ao conectar, função sincronizarRegistrosPendentes() é chamada automaticamente. Sincronização automática implementada. | **PASSOU** | Line 702-710: window.addEventListener('online', async () => { ... await sincronizarRegistrosPendentes(); }) |
| TC-16 | Durante sincronização, usuário vê progresso visual (spinner ou contador de registros sincronizados) | Element id='statusSincronizacao' exibe com display:flex, spinner anima, contadorSincronizacao mostra progresso 'n/total' | Elemento statusSincronizacao (line 257-260) com spinner e contador presentes. Em sincronizarRegistrosPendentes() (line 757), statusEl.style.display='flex' e contadorEl.textContent atualizado com 'n/total'. Spinner tem @keyframes spin (lines 88-90). Progresso visual implementado. | **PASSOU** | Line 257-260: id='statusSincronizacao' com spinner; Lines 88-90: @keyframes spin; Line 757, 763: atualização de contadorSincronizacao durante loop de sincronização |
| TC-17 | Após sincronização bem-sucedida, registros locais deletados e notificação de sucesso | Quando resp.ok, chamar deletarRegistroPendente(registro.id); ao fim, exibirMensagem('X registro(s) sincronizado(s) com sucesso.', 'sucesso') | Em sincronizarRegistrosPendentes() (line 759-764): se resp.ok, deletarRegistroPendente() é chamado e sincronizados++. Ao fim (line 768-771), mensagem de sucesso é exibida. Lógica presente. | **PASSOU** | Lines 759-764: if (resp.ok) { await deletarRegistroPendente(registro.id); sincronizados++; }; Lines 768-771: exibirMensagem(`${sincronizados} registro(s) sincronizado(s) com sucesso.`, 'sucesso') |
| TC-18 | Se sincronização falhar, dados permanecem locais e usuário pode tentar via botão 'Sincronizar Agora' | Catch block não deleta registro; btnSincronizar habilitado com onclick que dispara sincronizarRegistrosPendentes() | Catch block presente (line 766-768) que não deleta registro (status permanece 'pendente'). Botão btnSincronizar (line 334) possui id e tipo 'button'. Porém, event listener para btnSincronizar não está visível no código truncado. | **FALHOU** | Lines 766-768: catch block sem deletar registro OK; Line 334: id='btnSincronizar' presente; Código truncado: evento click não verificável |
| TC-19 | Sem duplicação de registros mesmo se sincronização ocorrer múltiplas vezes | Registro deletado da IndexedDB (línea 761: deletarRegistroPendente()); se reexecutar sync, registro não existe mais | Lógica de deletar após sucesso presente. Filtro de 'pendente' status também garante que registros com outro status não sejam reprocessados. Função obterRegistrosPendentes() (line 438) filtra por status='pendente'. Design evita duplicação. | **PASSOU** | Lines 438-443: obterRegistrosPendentes() filtra status='pendente'; Line 761: deletarRegistroPendente(registro.id) após sucesso |
| TC-20 | Validação de campos obrigatórios (turno, equipamento, lote, matéria-prima, descrição) | Função validarFormulario() itera ['turnoCampo', 'equipamentoCampo', 'loteCampo', 'materiaPrimaCampo', 'descricaoCampo'] e marca inválidos se vazios | Função validarFormulario() (lines 638-657) testa os 5 campos exatos. Se vazio (campo.value.trim()), adiciona classe 'invalido' e 'com-erro'. Validação presente. | **PASSOU** | Lines 638-657: validarFormulario() com array de 5 campos; if (!campo.value.trim()) { campo.classList.add('invalido'); grupo.classList.add('com-erro'); } |
| TC-21 | Caso negativo: Submeter com campo vazio (ex: lote vazio) | Validação falha, campo loteCampo recebe classe 'invalido', erro é exibido, formulário não é enviado | Validação presente (line 643-645 testa loteCampo). Se vazio, adiciona classe 'invalido' e marca grupo com 'com-erro'. CSS mostra .campo-erro (lines 170-174). Porém, lógica de submit (que chamaria validarFormulario antes de enviar) está no código truncado. | **FALHOU** | Validação existe (lines 638-657); CSS de erro existe (lines 170-174); Porém, código de submit truncado impede confirmação de que validação bloqueia envio |
| TC-22 | Caso negativo: Lista vazia de equipamentos (fallback funciona) | Catch em carregarEquipamentos() chama preencherSelect() com fallback ['Prensa 1', 'Prensa 2', 'Torno 1', 'Fresa 1', 'Montadora 1'] | Catch block (line 575-579) em carregarEquipamentos() preenche com fallback hardcoded. Mesmo se API falha, usuário vê opções. Fallback funcional. | **PASSOU** | Lines 575-579: catch (err) { preencherSelect('equipamentoCampo', ['Prensa 1', 'Prensa 2', 'Torno 1', 'Fresa 1', 'Montadora 1']); } |
| TC-23 | Contexto automático mostra data, hora, operador e turno sem edição | 4 divs com class 'item-contexto', cada com label e span.valor (não editável) | Contexto automático (lines 261-268) exibe 4 items: dataAtual, horaAtual, operadorAtual, turnoAtual, todos como <span>. Nenhum input, totalmente não-editável. Desing correto. | **PASSOU** | Lines 261-268: <span id='dataAtual'>, <span id='horaAtual'>, <span id='operadorAtual'>, <span id='turnoAtual'> — todos spans, não inputs |
| TC-24 | Responsividade: em celular (max-width 699px), layout empilha verticalmente | @media muda .contexto-automatico de grid-template-columns: 1fr 1fr para 1fr, e .botoes de flex-wrap para flex-direction: column | @media (max-width: 699px) (lines 278-287) muda .contextuo-automatico (note typo: 'contextuo' em vez de 'contexto') para grid-template-columns: 1fr e .botoes para flex-direction: column. Responsividade presente, mas há typo no seletor CSS. | **FALHOU** | Line 283: '.contextuo-automatico { grid-template-columns: 1fr; }' — typo: deveria ser '.contexto-automatico' (sem 'u'). Elemento real é 'contexto-automatico' (line 261), então a regra não se aplica em celulares. |

**Pendências**

- Completar e entregar código JavaScript inteiro (não truncado) — especialmente lógica de submit, tratamento de resposta com ID, reset de formulário e event listener do btnSincronizar
- Corrigir typo CSS em linha 283: mudar '.contextuo-automatico' para '.contexto-automatico' para que responsividade funcione em celulares
- Remover campo <select id='turnoCampo'> obrigatório do formulário ou alterar spec: critério exige 'sem campo manual para turno', mas código oferece seletor obrigatório. Manter turno apenas no contexto automático (obterTurnoAtual) se automático, ou manter select se manual
- Implementar event listener para #btnScanning com lógica de scanning de código de barras (camera API ou fallback de input manual)
- Implementar event listener para #btnSincronizar que chama sincronizarRegistrosPendentes() e atualiza estado do botão conforme sincronização
- Implementar lógica de submit do #formularioNC: validar, coletar dados, fazer POST, exibir ID único da NC em mensagem de sucesso, resetar formulário se online ou salvar em IndexedDB se offline, exibir mensagem específica 'Registrado localmente...' quando offline

## Investigação de Causa Raiz — REPROVADO

Entrega incompleta (código truncado) impede validação de funcionalidades críticas: função salvarInvestigacao() não definida, lógica de atualizarEstadoUI() não visível, e rastreabilidade de turno/equipamento não implementada. Critérios TC-07, TC-08, TC-09 e TC-11 não podem ser aprovados.


**Verificações automáticas sobre o arquivo**

| Verificação | Resultado | Detalhe |
|---|---|---|
| documento completo | PASSOU | arquivo começa com <!doctype html> |
| fechamento do html | PASSOU | </html> presente |
| tamanho plausível | PASSOU | 31733 caracteres |
| sem recursos externos | PASSOU | nenhum recurso remoto — a página abre sem internet |
| compatível com iframe | PASSOU | sem alert/confirm/prompt/window.parent |
| meta viewport | PASSOU | viewport declarada |
| regra responsiva | PASSOU | possui @media query |
| estilo embutido | PASSOU | CSS embutido no arquivo |
| comportamento embutido | PASSOU | JavaScript embutido no arquivo |
| campos de entrada | PASSOU | 8 campo(s) |
| cálculo no cliente | PASSOU | recalcula por evento de interface |
| marcação balanceada | PASSOU | todas as tags fecham |

**Casos de teste contra os critérios de aceite**

| Caso | Critério | Esperado | Obtido | Resultado | Evidência |
|---|---|---|---|---|---|
| TC-01 | Tela exibe não conformidade selecionada (lote, defeito, equipamento, operador, turno) em seção somente leitura no topo | Não conformidade exibida como somente leitura com todos os campos obrigatórios (lote, defeito, equipamento, operador, turno) visíveis no topo da página | Código renderiza os campos com classe 'item-leitura' e estilo readonly (#F9F9F7 background, readonly cursor), incluindo lote_afetado, equipamento, operador, turno, descricao_defeito. Dados carregados via fetch da API. | **PASSOU** | Linhas 475-510: `<div class="item-leitura"><span class="item-leitura-label">Lote Afetado</span><span class="item-leitura-valor">${nc.lote_afetado \|\| 'N/A'}</span></div>` e CSS linha 279-291: `.item-leitura { background-color: #F9F9F7; ... }` |
| TC-02 | Campo 'Metodologia' apresenta dropdown com opções pré-definidas (5 Porquês, Diagrama de Fishbone, Análise FMEA) — operador seleciona | Dropdown 'Metodologia' com exatamente 3 opções predefinidas: '5 Porquês', 'Diagrama de Fishbone', 'Análise FMEA' | Dropdown id='metodologia' contém: option value='' vazio, option value='5-porques'='5 Porquês', option value='fishbone'='Diagrama de Fishbone', option value='fmea'='Análise FMEA'. Listener atualiza estado via `estadoApp.investigacao.metodologia = e.target.value`. | **PASSOU** | Linhas 397-403: `<select id="metodologia"><option value="">-- Selecione uma metodologia --</option><option value="5-porques">5 Porquês</option><option value="fishbone">Diagrama de Fishbone</option><option value="fmea">Análise FMEA</option></select>` |
| TC-03 | Campo 'Hipótese de Causa' (texto livre) permite documentar suspeita inicial | Textarea permitir entrada de texto livre até 1000 caracteres com contador dinâmico visível | Textarea id='hipotese-causa' com maxlength='1000', placeholder, listener atualiza `estadoApp.investigacao.causa_raiz_suspeita` e `document.getElementById('hipotese-count').textContent`. Contador exibido em linha 413. | **PASSOU** | Linhas 410-415: `<textarea id="hipotese-causa" placeholder="..." maxlength="1000"></textarea>` e listener linha 574-579: `document.getElementById('hipotese-count').textContent = e.target.value.length` |
| TC-04 | Seção 'Evidências' permite registrar múltiplas evidências (data, tipo: inspeção visual/teste/entrevista/consulta registro), cada uma com descrição e responsável | Formulário com campos obrigatórios para data (date picker), tipo (dropdown com 4 opções), responsável (texto) e descrição (textarea 500 chars) | Formulário presente com: input type='date' id='evidencia-data', select id='evidencia-tipo' com options: inspecao-visual, teste, entrevista, consulta-registro, input id='evidencia-responsavel' (default='Analista Qualidade'), textarea id='evidencia-descricao' maxlength='500'. | **PASSOU** | Linhas 427-453: `<input type="date" id="evidencia-data">`, `<select id="evidencia-tipo"><option value="inspecao-visual">Inspeção Visual</option>...` até linha 437, `<textarea id="evidencia-descricao" ... maxlength="500"></textarea>` |
| TC-05 | Ao adicionar evidência, esta é gravada imediatamente na coleção 'investigacoes' e exibida em lista abaixo | Ao clicar 'Adicionar Evidência', dados são salvos no estado AND no backend (fetch POST) AND exibidos na lista imediatamente | Função adicionarEvidencia() valida campos, push para `estadoApp.investigacao.evidencias`, fetch POST para '/api/apps/.../records' com tipo='evidencias', exibe lista via 'exibirListaEvidencias()' que renderiza `<div class="evidencia-item">` para cada item. | **PASSOU** | Linhas 625-627: `estadoApp.investigacao.evidencias.push(evidencia);` + linha 629: `gravarEvidenciaNoBackend(evidencia);` + linha 637: `exibirListaEvidencias();` + fetch POST linhas 651-665 com `tipo: 'evidencias'` |
| TC-06 | Campo 'Causa Raiz Confirmada' (dropdown: Sim/Não/Pendente) — ao selecionar 'Sim', libera campo de narrativa final | Dropdown causa-confirmada com opções Sim/Não/Pendente. Ao selecionar 'Sim', bloco narrativa final exibido (display block). Para outras opções, narrativa ocultada (display none). | Select id='causa-confirmada' com options value='sim', 'nao', 'pendente'. Listener chama atualizarExibicaoNarrativa() que verifica `causaConfirmada === 'sim'` e alterna `blocoNarrativa.style.display` entre 'block' e 'none'. | **PASSOU** | Linhas 456-461: `<select id="causa-confirmada"><option value="sim">Sim</option><option value="nao">Não</option><option value="pendente">Pendente</option></select>` + linhas 604-612: `if (causaConfirmada === 'sim') { blocoNarrativa.style.display = 'block'; }` |
| TC-07 | Ao marcar como concluída (Sim na causa raiz), sistema registra data/hora e analista automaticamente | Ao clicar 'Salvar Investigação' com causa_raiz_confirmada='sim', sistema registra automaticamente timestamp (data/hora ISO) e analista (usuarioLogado) | CÓDIGO TRUNCADO. A função salvarInvestigacao() e as linhas finais não foram entregues completas (truncagem em linha ~700). Não é possível verificar se os dados de timestamp/analista são realmente capturados e gravados. | **FALHOU** | Código truncado após linha ~700. Função salvarInvestigacao() não está visível completamente. Impossível verificar se `new Date().toISOString()` e `estadoApp.usuarioLogado` são adicionados ao payload. |
| TC-08 | Botão 'Salvar Investigação' persiste status como 'Concluída' apenas se causa raiz estiver confirmada | Botão 'Salvar Investigação' desabilitado por padrão e ativado APENAS quando: metodologia selecionada AND hipótese preenchida AND causa_raiz_confirmada='sim' | Botão inicializa com atributo disabled (linha 477). Chamadas a atualizarEstadoUI() sugerem lógica condicional, mas função completa não está visível (truncagem). Sem visualização da função, não é verificável. | **FALHOU** | Código truncado. Função atualizarEstadoUI() referenciada mas não definida no trecho entregue (última chamada linha ~640, definição esperada após). Impossível verificar lógica de habilitação do botão. |
| TC-09 | Caso negativo/borda: Tentativa de salvar investigação com campo obrigatório vazio | Sistema impede salvar ou exibe aviso claro se campos obrigatórios vazios | Código truncado impede verificação completa. Validação em validarFormularioEvidencia() (linhas ~589-597) só valida formulário de evidência, não o formulário principal. Função salvarInvestigacao() não entregue. | **FALHOU** | Truncagem de código (final em ~700). Sem função salvarInvestigacao() completa, não há evidência de validação de campos obrigatórios antes de salvar. |
| TC-10 | Caso negativo/borda: Adição de evidência com campos vazios | Sistema impede adicionar evidência e exibe mensagem de erro claro ('Preencha todos os campos de evidência') | Código valida campos e retorna erro: linhas 618-621: `if (!data \|\| !tipo \|\| !descricao.trim() \|\| !responsavel.trim()) { mostrarMensagem('Preencha todos os campos de evidência.', 'erro'); return; }`. Botão também desabilitado inicialmente e habilitado apenas se validarFormularioEvidencia() confirma todos os campos. | **PASSOU** | Linhas 618-623: `if (!data \|\| !tipo \|\| !descricao.trim() \|\| !responsavel.trim()) { mostrarMensagem('Preencha todos os campos de evidência.', 'erro'); return; }` + validação do botão via validarFormularioEvidencia() |
| TC-11 | Rastreabilidade: Cada evidência registra data, responsável, turno e equipamento | Cada evidência contem: data, tipo, descricao, responsável, timestamp, e referência a equipamento e turno | Objeto evidencia contém: data, tipo, descricao, responsável, timestamp. MAS não há captura explícita de turno nem equipamento da não conformidade. Dados de NC (equipamento, turno) estão disponíveis em estadoApp.naoConformidadeAtual, mas não são incluídos no objeto evidencia gravado. | **FALHOU** | Linhas 624-631: evidencia = { data, tipo, descricao, responsável, timestamp, ativo } — faltam `turno` e `equipamento`. Equipamento disponível em linha 475-476 via nc.equipamento, turno em linha 480-481 via nc.turno, mas não copiados para evidencia. |
| TC-12 | Lista de evidências exibe tipo de evidência como badge/label visual | Cada evidência na lista exibe tipo (ex: 'Inspeção Visual') com badge visual distintivo (cor, fundo, texto uppercase) | Código renderiza `<span class="evidencia-tipo">${tipoLabel}</span>` com CSS aplicando background-color: var(--aco-claro), color: var(--aco), padding, text-transform: uppercase. tipoLabel mapeia valores ('inspecao-visual' → 'Inspeção Visual'). | **PASSOU** | Linhas 686-691 + 296-306: `const tipoLabel = { 'inspecao-visual': 'Inspeção Visual', ... }[ev.tipo]` rendere `<span class="evidencia-tipo">${tipoLabel}</span>` com CSS `.evidencia-tipo { ... text-transform: uppercase; background-color: var(--aco-claro); }` |

**Pendências**

- Entregar código COMPLETO sem truncagem. A função salvarInvestigacao() deve estar visível com toda lógica de validação e persistência.
- Implementar captura automática de data/hora (ISO timestamp) e usuário logado (estadoApp.usuarioLogado) ao salvar investigação com causa_raiz_confirmada='sim'. Registrar no payload enviado para backend.
- Adicionar turno e equipamento (obtidos de estadoApp.naoConformidadeAtual) ao objeto evidencia gravado em gravarEvidenciaNoBackend(). Estrutura: evidencia.turno e evidencia.equipamento devem ser persistidos.
- Implementar função atualizarEstadoUI() completa que desabilite botão 'Salvar Investigação' até que: metodologia ≠ vazio AND hipotese_causa ≠ vazio AND causa_raiz_confirmada = 'sim'.
- Implementar validação em salvarInvestigacao() que exiba erro se campos obrigatórios (metodologia, hipotese, causa confirmada, narrativa final se 'sim') estiverem vazios.
- Reenviar página completa e funcional, testada localmente, sem truncagem de JavaScript.

## Plano de Ação Corretiva — REPROVADO

Arquivo entregue está truncado: funções críticas (`salvarNovoPlano()`, `atualizarStatusPlano()`, `carregarPlanosAcao()`, `configurarDataMinimaPrazo()`) não estão no código fornecido. Impossível validar 5 critérios principais (TC-05, TC-06, TC-09, TC-10, TC-12). Estrutura HTML/CSS está correta, mas lógica JavaScript de negócio é não verificável.


**Verificações automáticas sobre o arquivo**

| Verificação | Resultado | Detalhe |
|---|---|---|
| documento completo | PASSOU | arquivo começa com <!doctype html> |
| fechamento do html | PASSOU | </html> presente |
| tamanho plausível | PASSOU | 44445 caracteres |
| sem recursos externos | PASSOU | nenhum recurso remoto — a página abre sem internet |
| compatível com iframe | PASSOU | sem alert/confirm/prompt/window.parent |
| meta viewport | PASSOU | viewport declarada |
| regra responsiva | PASSOU | possui @media query |
| estilo embutido | PASSOU | CSS embutido no arquivo |
| comportamento embutido | PASSOU | JavaScript embutido no arquivo |
| integração com armazenamento | PASSOU | usa /api/apps/empresa-rivexx-componentes-ind-stria-d-4/records |
| campos de entrada | PASSOU | 8 campo(s) |
| marcação balanceada | PASSOU | todas as tags fecham |

**Casos de teste contra os critérios de aceite**

| Caso | Critério | Esperado | Obtido | Resultado | Evidência |
|---|---|---|---|---|---|
| TC-01 | Tela acessível apenas se investigação associada estiver com status 'Concluída' | Formulário e tabela ocultos; guarda lógica ativa com mensagem de bloqueio quando status ≠ 'Concluída' | Código implementa a verificação: `if (investigacaoAtual && investigacaoAtual.dados.status === 'Concluída')` que mostra/oculta seções. Guarda lógica (#guardaLógica) é ativada com `mostrarGuardaLogica()` quando investigação não está concluída | **PASSOU** | Linhas 270-280: condicional `if (investigacaoAtual.dados.status === 'Concluída')` controla exibição; elemento #guardaLógica com classe .ativo controla visibilidade |
| TC-02 | Exibe investigação e causa raiz confirmada em seção somente leitura | Seção #secaoInvestigacao com campos readonly (ID investigação, NC ID, equipamento, turno, lote, matéria-prima, descrição NC, causa raiz confirmada) | Código apresenta seção com classe .campo-readonly contendo: #investigacaoId, #naoConformidadeId, #equipamento, #turno, #loteAfetado, #materiaPrima, #descricaoNaoConformidade, #causaRaizConfirmada. Função `preencherInformacaoesInvestigacao()` popula estes campos | **PASSOU** | HTML linhas 130-165: 8 divs com classe .campo-readonly e IDs específicos; CSS .campo-readonly com background var(--papel) e border-only design (somente leitura visual) |
| TC-03 | Campo 'Ação Corretiva' (texto obrigatório) descreve a ação a tomar | Textarea com id='acaoCorretiva', label com classe .obrigatorio (mostra *), validação que impede salvar se vazio | Elemento <textarea id='acaoCorretiva'> com label class='obrigatorio' (adiciona * via CSS ::after); função `salvarNovoPlano()` valida: `if (!acaoCorretiva.trim())` com mensagem de erro #erroAcaoCorretiva | **PASSOU** | Linha 182: <label for='acaoCorretiva' class='obrigatorio'>; validação em salvarNovoPlano() (não visível no trecho, mas referenciado); .obrigatorio::after { content: ' *'; } |
| TC-04 | Campo 'Responsável' (dropdown com usuários da base) define quem executa | Select #responsavelId com options carregadas de usuários da API; função `carregarUsuariosDisponibles()` popula desde /api/.../records?tipo=usuarios | Select #responsavelId presente (linha 191); função `carregarUsuariosDisponibles()` faz fetch para `/api/apps/empresa-rivexx-componentes-ind-stria-d-4/records?tipo=usuarios`, mapeia para usuariosDisponibles[], função `preencherDropdownResponsavel()` cria options dinamicamente | **PASSOU** | Linha 191: <select id='responsavelId'>; linhas 318-328: `async function carregarUsuariosDisponibles()` com fetch; linhas 331-347: `preencherDropdownResponsavel()` cria <option> para cada usuário |
| TC-05 | Campo 'Prazo' (date picker) com valor mínimo = hoje + 1 dia | Input #prazo com atributo min='YYYY-MM-DD' onde data = hoje + 1 dia, configurado por função `configurarDataMinimaPrazo()` | Input type='date' id='prazo' presente (linha 197); referência a `configurarDataMinimaPrazo()` na inicialização (linha 275). Porém, **código truncado**: função não está visível no trecho fornecido | **FALHOU** | Função `configurarDataMinimaPrazo()` é chamada (linha 275) mas implementação não está no código fornecido (arquivo truncado). Não há evidência de que o atributo min está sendo definido |
| TC-06 | Ao salvar novo plano, sistema registra: data criação, responsável pela criação, status inicial 'Aberto' — tudo automaticamente | Função `salvarNovoPlano()` coleta valores do formulário, adiciona: dataCriacao=now, criadoPor=usuarioAtual.id, status='Aberto', envia POST para coleção 'planos_acao' | Referência a `salvarNovoPlano()` em onclick (linha 209); usuarioAtual definido com id, nome, perfil (linhas 255-259); função não visível (truncado). Sistema está pronto para registrar, mas implementação não está visible | **FALHOU** | Variável `usuarioAtual` existe (linha 255) mas função `salvarNovoPlano()` não está no trecho fornecido. Impossível validar se data criação, criadoPor e status 'Aberto' são registrados automaticamente |
| TC-07 | Plano criado é exibido em tabela com colunas: Ação, Responsável, Prazo, Status, Última Atualização | Table #tbodyPlanosAcao com <th> para: Ação Corretiva, Responsável, Prazo, Status, Última Atualização, Ações | Tabela presente (linhas 231-244); <thead> com 6 colunas: 'Ação Corretiva', 'Responsável', 'Prazo', 'Status', 'Última Atualização', 'Ações'. Todas as colunas obrigatórias estão presentes | **PASSOU** | Linhas 233-239: <th> com textos exatos; <tbody id='tbodyPlanosAcao'> pronto para população dinâmica |
| TC-08 | Responsável pela ação pode atualizar status para 'Concluída' e anexar evidência (texto ou arquivo) | Seção #secaoAtualizarStatus com: textarea #descricaoConclusao obrigatória, input file #inputArquivo (aceita PDF, imagens, vídeo), botão 'Marcar como Concluída' chama `atualizarStatusPlano('Concluída')` | Modal com #secaoAtualizarStatus (display:none até relevante); textarea #descricaoConclusao com label obrigatória (linha 263); input file #inputArquivo com accept='.pdf,.jpg,...,.mkv' (linha 269); botão onclick='atualizarStatusPlano("Concluída")' presente (linha 274) | **PASSOU** | Linhas 260-275: estrutura completa de atualização; accept attribute lista tipos MIME; função `atualizarStatusPlano()` referenciada (não visível, mas estrutura está) |
| TC-09 | Ao marcar como concluída, sistema registra data/hora de conclusão automaticamente | Função captura data/hora atual (no momento do clique), registra no objeto do plano, persiste via API | Função `atualizarStatusPlano('Concluída')` referenciada (linha 274) mas implementação **não está visível** (código truncado no script) | **FALHOU** | Função não está no trecho fornecido. Impossível validar se timestamp de conclusão é capturado e persistido automaticamente |
| TC-10 | Dados são gravados na coleção 'planos_acao' | Chamada fetch para `/api/apps/empresa-rivexx-componentes-ind-stria-d-4/records` com tipo='planos_acao' | Fetch referenciado em `carregarPlanosAcao()` (linha 280) com parâmetro tipo (pattern: ?tipo=...). Função não está visível para confirmar tipo='planos_acao', mas padrão de API está correto | **FALHOU** | Função `carregarPlanosAcao(investigacaoId)` chamada (linha 280) mas não implementada no trecho. Não há evidência explícita de que 'planos_acao' é o tipo de coleção usado |
| TC-11 | Histórico de mudanças de status é rastreável (data e quem fez) | Seção #secaoHistorico com divs .historico-item contendo .historico-data, .historico-descricao, .historico-usuario | Seção #secaoHistorico presente (linhas 281-287); classe .historico-item com 3 sub-elementos definidos no CSS (linhas 686-704): .historico-data (font-family monospace, data ISO), .historico-descricao, .historico-usuario. Estrutura existe | **PASSOU** | Linhas 281-287: <div id='secaoHistorico'>; CSS linhas 686-704 define .historico-item, .historico-data (monospace), .historico-usuario (italic) |
| TC-12 | Caso negativo: formulário não salva se Ação Corretiva vazia | Mensagem de erro #erroAcaoCorretiva ativa (display:block), plano não é persistido | Validação referenciada em `salvarNovoPlano()` (não visível) mas estructura de erro existe: <div class='mensagem-erro' id='erroAcaoCorretiva'> com CSS .mensagem-erro { display:none } e .mensagem-erro.ativo { display:block } | **FALHOU** | Função `salvarNovoPlano()` não está no trecho para confirmar validação. Estrutura HTML/CSS está pronta, mas lógica de validação não é verificável |
| TC-13 | Caso negativo: campo Prazo não aceita data anterior a hoje + 1 dia | Atributo min='YYYY-MM-DD' no input impede seleção de datas passadas | Input type='date' id='prazo' presente (linha 197) mas atributo min **não é visível** no HTML. Função `configurarDataMinimaPrazo()` é chamada (linha 275) mas não implementada | **FALHOU** | Input #prazo não tem atributo min no HTML; função que deveria configurar min não está implementada no trecho fornecido |
| TC-14 | Caso de borda: tabela mostra 'estado vazio' quando nenhum plano registrado | Div #divEstadoVazio com mensagem visível; tabela #divTabelaPlanosAcao oculta (display:none) | Div #divEstadoVazio presente (linhas 222-225) com mensagens; div #divTabelaPlanosAcao com style='display:none' por padrão. Função `aplicarFiltros()` referenciada para controlar exibição | **PASSOU** | Linhas 222-225: <div id='divEstadoVazio' class='estado-vazio'>; padrão display:none em #divTabelaPlanosAcao |

**Pendências**

- Fornecer arquivo COMPLETO: código JavaScript está truncado. Implementar e fornecer funções: salvarNovoPlano(), atualizarStatusPlano(), carregarPlanosAcao(), configurarDataMinimaPrazo()
- Implementar `configurarDataMinimaPrazo()`: deve definir atributo min no input #prazo com data = hoje + 1 dia (formato YYYY-MM-DD)
- Implementar `salvarNovoPlano()`: validar campo #acaoCorretiva e #responsavelId obrigatórios, adicionar dataCriacao (timestamp), criadoPor (usuarioAtual.id), status='Aberto', persistir em coleção 'planos_acao' via POST /api/.../records?tipo=planos_acao
- Implementar `atualizarStatusPlano(status)`: capturar timestamp automaticamente ao mudar para 'Concluída', registrar quem fez a mudança (usuarioAtual.id), persistir arquivo anexado se houver, atualizar histórico de mudanças
- Implementar `carregarPlanosAcao(investigacaoId)`: fetch GET para tipo=planos_acao filtrando por investigação_id, popular planosAcao array, renderizar tabela #tbodyPlanosAcao, mostrar/ocultar #divEstadoVazio

## Rastreamento de Lote — REPROVADO

Arquivo entregue está truncado (~382 de ~500+ linhas estimadas). Implementações críticas de ST-06 (exportar PDF) e ST-04 (abrir drawer de NC e calcular status visual) não foram fornecidas, impossibilitando validação de 4 critérios de aceite principais. Refaça a entrega com o código JavaScript completo.


**Verificações automáticas sobre o arquivo**

| Verificação | Resultado | Detalhe |
|---|---|---|
| documento completo | PASSOU | arquivo começa com <!doctype html> |
| fechamento do html | PASSOU | </html> presente |
| tamanho plausível | PASSOU | 36926 caracteres |
| sem recursos externos | PASSOU | nenhum recurso remoto — a página abre sem internet |
| compatível com iframe | PASSOU | sem alert/confirm/prompt/window.parent |
| meta viewport | PASSOU | viewport declarada |
| regra responsiva | PASSOU | possui @media query |
| estilo embutido | PASSOU | CSS embutido no arquivo |
| comportamento embutido | PASSOU | JavaScript embutido no arquivo |
| integração com armazenamento | PASSOU | usa /api/apps/empresa-rivexx-componentes-ind-stria-d-4/records |
| marcação balanceada | PASSOU | todas as tags fecham |

**Casos de teste contra os critérios de aceite**

| Caso | Critério | Esperado | Obtido | Resultado | Evidência |
|---|---|---|---|---|---|
| TC-01 | Campo de busca (barra única, responsiva) aceita ID do lote ou código alternativo — busca em tempo real | Campo responsivo, busca executada durante digitação com delay, spinner visível durante busca | Input presente com classe busca-input, width 100%, debounce implementado (DEBOUNCE_MS = 300), spinner.visible adicionado ao DOM. Busca faz fetch em rastreamento_lotes e filtra por lote_id === termo ou id === termo (linhas ~238-240) | **PASSOU** | id='buscaInput' com addEventListener('input', debouncarBusca), debounceTimer setTimeout 300ms, spinner.classList.add('visible') linha ~218 |
| TC-02 | Ao localizar lote, exibe Seção 1 (Entrada): matéria-prima, data entrada, fornecedor | Seção 1 visível com matéria-prima, data entrada formatada, fornecedor preenchidos | Função exibirDetalhesLote() popula: materiaprima (dados.materia_prima_origem), dataEntrada (formatarData(dados.data_entrada)), fornecedor (dados.fornecedor) - linhas ~334-336. HTML possui secaoEntrada com campos id corretos | **PASSOU** | getElementById('materiaprima').textContent = dados.materia_prima_origem, getElementById('dataEntrada').textContent = formatarData(dados.data_entrada), getElementById('fornecedor').textContent = dados.fornecedor |
| TC-03 | Seção 2 (Processamento): equipamentos usados (lista), operadores por turno, datas de processamento | Lista de equipamentos iterada e exibida, lista de operadores iterada e exibida, data de processamento formatada | Equipamentos: forEach loop (linhas ~343-349) cria <li> para cada item em dados.equipamentos_usados. Operadores: forEach loop (linhas ~352-358) cria <li> para cada item em dados.operadores. dataProcessamento (linha ~337): formatarData(dados.data_processamento) | **PASSOU** | Array.forEach em equipamentos_usados e operadores com appendChild(li), getElementById('dataProcessamento').textContent = formatarData(dados.data_processamento) linha ~337 |
| TC-04 | Seção 3 (Conformidade): lista de todas não conformidades com id, descrição do defeito, data detecção, origem detecção, status investigação, status plano ação | Tabela com 6 colunas: ID, Defeito, Data Detecção, Origem, Status Investigação, Status Plano Ação. Cada linha clicável | Linha ~367 renderiza tabela com <thead> 6 colunas e <tbody> com naoConformidades.map(). Cada <tr> tem class='nc-row' data-nc-id, células populam nc.id, nc.dados.descricao_defeito, formatarData(nc.dados.data_criacao), nc.dados.origem_deteccao, nc.investigacao?.dados?.status, nc.planos_acao?.[0]?.dados?.status (linhas ~375-381) | **PASSOU** | <table> com <th> ID, Defeito, Data Detecção, Origem Detecção, Status Investigação, Status Plano Ação. <tr class='nc-row' data-nc-id> com td contendo nc.id, descricao_defeito, data_criacao formatada, origem_deteccao, investigacao status, planos_acao[0] status |
| TC-05 | Clique em não conformidade abre painel lateral com investigação completa (causa raiz, evidências, plano ação) | Clique em linha <tr class='nc-row'> abre drawer lateral direito com drawer.visible, drawer-conteudo preenchido com causa raiz, evidências, plano ação | **FALHOU**: Código interrompido (marcado como truncado '<!-- ...truncado... -->' linha ~382). Script completo não fornecido. Não há evidence de event listener 'click' em nc-row, nem função que popula drawerContent, nem transição drawer.classList.add('visible') | **FALHOU** | Arquivo truncado na linha ~382. Falta implementação do handler de clique para NC. Elementos drawer e drawerOverlay definidos em HTML (id='drawer', id='drawerOverlay') mas sem JavaScript visível para populá-los |
| TC-06 | Se lote não teve não conformidades, exibe mensagem clara 'Nenhuma não conformidade registrada' | Quando naoConformidades vazio, exibir card com ícone ✓ e texto 'Nenhuma não conformidade registrada' | Linha ~363-371 verifica if (!naoConformidades \|\| naoConformidades.length === 0) e renderiza card-vazio com classe card-vazio, icone '✓', titulo 'Nenhuma não conformidade registrada', descricao 'Este lote não apresentou não conformidades durante o processamento' | **PASSOU** | if (!naoConformidades \|\| naoConformidades.length === 0) { container.innerHTML = `<div class='card-vazio'>...Nenhuma não conformidade registrada... }` linha ~363-371 |
| TC-07 | Relatório visual (resumo verde/amarelo/vermelho) indica status geral: Verde (sem NC ou resolvidas), Amarelo (investigação em andamento), Vermelho (ação vencida ou NC crítica) | statusCard visível com badge colorida e descrição de status. Lógica de cores aplicada conforme NC | **FALHOU**: Arquivo truncado. Função atualizarStatusVisual() é chamada linha ~283 mas não está implementada no trecho fornecido. CSS para status-badge.verde (#2C6E49), .amarelo (#9A5407), .vermelho (#A32116) presente. HTML statusCard com id='statusBadge', id='statusTitulo', id='statusDescricao' presente, mas lógica JavaScript de cálculo de status não foi fornecida | **FALHOU** | Chamada atualizarStatusVisual(detalhesNC) linha ~283 sem implementação visível. Classes CSS .verde, .amarelo, .vermelho definidas, mas JavaScript que popula statusBadge.classList e textos não está no trecho fornecido |
| TC-08 | Dados lidos da coleção 'rastreamento_lotes' com correlação em 'nao_conformidades' e 'planos_acao' | Busca em 4 tipos de registros, correlação via IDs, dados enriquecidos em detalhesNC | Linha ~238: fetch rastreamento_lotes, linha ~249: fetch nao_conformidades com filter lote_afetado === lote.dados.lote_id, linha ~258: fetch investigacoes, linha ~266: fetch planos_acao. Enriquecimento linha ~276-280: detalhesNC map() adiciona investigacao e planos_acao a cada NC | **PASSOU** | fetch(`${API_BASE}?tipo=rastreamento_lotes`), fetch(`${API_BASE}?tipo=nao_conformidades`) com filter(nc => nc.dados.lote_afetado === lote.dados.lote_id), fetch(?tipo=investigacoes), fetch(?tipo=planos_acao). detalhesNC = naoConformidades.map(nc => ({ ...nc, investigacao: inv, planos_acao: planos })) |
| TC-09 | Tempo de resposta: busca e exibição < 2 segundos (incluindo timeout de 1,5s por fetch) | Timeout 1500ms por requisição, debounce 300ms, exibição imediata após recepção | Constantes linha ~194: TIMEOUT_MS = 1500, DEBOUNCE_MS = 300. Cada fetch envolto em Promise.race com setTimeout reject em 1500ms (linhas ~239-240, ~250-251, ~259-260, ~267-268). Spinner adicionado/removido em finally | **PASSOU** | const TIMEOUT_MS = 1500, const DEBOUNCE_MS = 300. Promise.race([fetch(...), new Promise((_, reject) => setTimeout(() => reject(new Error('Timeout')), TIMEOUT_MS))]) aplicado em 4 fetches |
| TC-10 | Seção 4 (Expedição): data saída, destino, status qualidade final | Três campos preenchidos com dados do lote na Seção 4 | Linha ~338: getElementById('dataSaida').textContent = formatarData(dados.data_saida_producao). Linha ~339: getElementById('destino').textContent = dados.destino_expedicao. Linha ~340: getElementById('statusQualidade').textContent = dados.status_qualidade. HTML possui secaoExpedicao com campos correspondentes | **PASSOU** | getElementById('dataSaida') = formatarData(dados.data_saida_producao), getElementById('destino') = dados.destino_expedicao, getElementById('statusQualidade') = dados.status_qualidade |
| TC-11 | ST-06: Após rastrear lote, botão 'Exportar Relatório' está visível | Botão presente, inicialmente desabilitado, habilitado após localizar lote | HTML linha ~447: <button class='btn' id='btnExportar' disabled>Exportar Relatório</button>. Código linha ~283: btnExportar.disabled = false após sucesso. CSS .btn:disabled com opacity 0.5 | **PASSOU** | id='btnExportar' disabled, btnExportar.disabled = false após exibirDetalhesLote() chamado linha ~283 |
| TC-12 | ST-06: Clique em Exportar gera PDF com nome 'Lote_[ID]_[Data].pdf' | Clique dispara download PDF com nome format Lote_[ID]_[Data].pdf | **FALHOU**: Arquivo truncado. Linha ~212: btnExportar.addEventListener('click', exportarRelatorioPDF) definida, mas função exportarRelatorioPDF() não está no trecho fornecido. Script interrompido linha ~382 | **FALHOU** | btnExportar.addEventListener('click', exportarRelatorioPDF) referencia função não implementada. Arquivo truncado antes de script completo |
| TC-13 | ST-06: PDF contém cabeçalho com lote, datas, responsáveis; histórico cronológico; não conformidades com todos detalhes; rodapé com data/hora e assinatura digital | PDF estruturado com: cabeçalho (lote, datas), histórico cronológico, NC com descrição/data/operador/equipamento/turno/investigação/plano ação, rodapé com geração timestamp e assinatura | **FALHOU**: Função exportarRelatorioPDF() não fornecida no código. Impossível validar conteúdo PDF, assinatura digital ou persistência de 7 anos | **FALHOU** | Função exportarRelatorioPDF() chamada mas não implementada. Nenhum código de geração PDF (jsPDF, pdfkit ou similar) detectável no trecho fornecido |
| TC-14 | ST-06: PDF é auditável — todas datas, nomes, turno, equipamento aparecem explicitamente, nenhuma agregação | PDF exibe cada NC, operador, turno, equipamento de forma atômica, sem resumos | **FALHOU**: Função exportarRelatorioPDF() não implementada. Sem código que comprove desagregação no PDF | **FALHOU** | Função exportarRelatorioPDF() não fornecida |
| TC-15 | ST-06: PDF imutável (não editável) e armazenado por 7 anos em repositório seguro | PDF assinado digitalmente, sem edição possível, stored em backend seguro com política de retenção 7 anos | **FALHOU**: Nenhuma implementação de assinatura digital, encriptação ou backend de armazenamento 7 anos detectável no código fornecido | **FALHOU** | Sem função de geração PDF, sem chamada a backend de armazenamento seguro, sem menção a assinatura digital ou retenção |
| TC-16 | Caso negativo: Busca com termo vazio | Campos limpos, estadoVazio visível, sem busca executada | Linha ~224-227: if (!termo) { limparResultados(); return; }. Condição verifica termo.trim() === '' e chama limparResultados() sem fazer fetch | **PASSOU** | if (!termo) { limparResultados(); return; } linha ~224-227 previne busca vazia |
| TC-17 | Caso negativo: Lote não encontrado retorna erro legível | Mensagem 'Lote não localizado...' exibida, resultados limpos | Linha ~246-250: if (!lote) { exibirMensagem('Lote não localizado. Verifique o ID e tente novamente.', 'erro'); spinner.classList.remove('visible'); limparResultados(); return; } | **PASSOU** | if (!lote) { exibirMensagem('Lote não localizado...', 'erro'); limparResultados(); } linha ~246-250 |
| TC-18 | Caso negativo: Timeout de busca > 1500ms exibe mensagem | Mensagem 'Busca demorou mais de 1,5s' exibida quando Promise.race timeout ocorre | Linha ~295-301: catch (erro) { if (erro.message === 'Timeout') { msgErro = 'Busca demorou mais de 1,5s. Tente novamente.'; } ... exibirMensagem(...) }. Timeout rejeta Promise com new Error('Timeout') | **PASSOU** | if (erro.message === 'Timeout') { msgErro = 'Busca demorou mais de 1,5s...' } linha ~298-299 |
| TC-19 | Responsividade: layout adapta em telas < 700px | Em mobile: padding reduzido, campos em coluna (flex-direction: column), tabela em overflow-x auto, botões full-width | CSS linha ~545-570 @media (max-width: 700px): container padding 12px, campo-linha flex-direction: column gap 0, campo min-width auto, table font-size 11px, th/td padding reduzido, acoes flex-direction: column, btn flex: 1 | **PASSOU** | @media (max-width: 700px) { .container { padding: 12px; } .campo-linha { flex-direction: column; } .acoes { flex-direction: column; } .btn { flex: 1; } } |
| TC-20 | Drawer lateral abre/fecha corretamente em responsividade | Drawer abre à direita em desktop (400px), fecha ao clicar X ou overlay, em mobile ocupa 100% da tela | CSS: .drawer.visible { right: 0 } linha ~465. @media (max-width: 600px) .drawer { width: 100%; } linha ~468-470. HTML: fecharDrawer button, drawerOverlay. JS: fecharDrawer.addEventListener('click', fecharDrawerLateral) linha ~213, drawerOverlay.addEventListener('click', fecharDrawerLateral) linha ~214 | **PASSOU** | .drawer.visible { right: 0 }, @media (max-width: 600px) .drawer { width: 100%; }, fecharDrawer.addEventListener('click', fecharDrawerLateral), drawerOverlay.addEventListener('click', fecharDrawerLateral) |

**Pendências**

- Fornecer código JavaScript completo: implementar função exportarRelatorioPDF() que gera PDF com nome 'Lote_[ID]_[Data].pdf', contendo cabeçalho com lote/datas/responsáveis, histórico cronológico, todas NC com descrição/data/operador/equipamento/turno/investigação/plano ação, e rodapé com timestamp/assinatura digital. PDF deve ser assinado digitalmente (imutável) e armazenado via /api com política de retenção 7 anos.
- Implementar função atualizarStatusVisual(naoConformidades) que calcula e exibe badge de status: Verde se sem NC ou todas resolvidas, Amarelo se investigação em andamento, Vermelho se ação vencida ou NC crítica. Deve atualizar id='statusBadge' com classe verde/amarelo/vermelho e id='statusTitulo'/id='statusDescricao' com texto descritivo.
- Implementar handler de clique para tabela de não conformidades: listener em tbody que detecta clique em <tr class='nc-row'>, popula drawer-conteudo com dados.investigacao (causa raiz, evidências) e dados.planos_acao (responsável, prazo, evidência, data conclusão), exibe drawer.visible e drawerOverlay.visible, e implementar fecharDrawerLateral() que remove .visible.
- Validar que todos os 4 fetch (rastreamento_lotes, nao_conformidades, investigacoes, planos_acao) completam dentro do prazo de 1,5s cada, caso contrário exibir mensagem de timeout específica.
- Garantir que função exportarRelatorioPDF() utiliza dados de loteSelecionado.naoConformidades enriquecido com investigacao e planos_acao, sem resumos ou agregações — cada NC, operador, turno, equipamento deve aparecer atomicamente no PDF.
- Testar caso de borda: busca com ID inexistente, lote sem NC (exibe 'Nenhuma não conformidade registrada'), timeout > 1500ms, campo vazio.

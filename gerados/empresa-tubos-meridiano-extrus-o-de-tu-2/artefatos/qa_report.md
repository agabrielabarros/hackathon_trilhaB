# Relatório de QA — Controle de Perda de Resina

`run empresa-tubos-meridiano-extrus-o-de-tu-2-86146f` · **QA Agent**

**Casos executados:** 63 · **Passou:** 52 · **Falhou:** 11 · **Retrabalhos solicitados:** 8


## Lançamento de Turno — REPROVADO

O arquivo HTML foi entregue truncado, impossibilitando validação do critério mais crítico (redirecionamento pós-envio ST-01 e persistência de dados ST-05). A verificação automática já registrou falha de integração com a API. Além disso, mensagens offline obrigatórias não foram encontradas no código visível.


**Verificações automáticas sobre o arquivo**

| Verificação | Resultado | Detalhe |
|---|---|---|
| documento completo | PASSOU | arquivo começa com <!doctype html> |
| fechamento do html | PASSOU | </html> presente |
| tamanho plausível | PASSOU | 28506 caracteres |
| sem recursos externos | PASSOU | nenhum recurso remoto — a página abre sem internet |
| compatível com iframe | PASSOU | sem alert/confirm/prompt/window.parent |
| meta viewport | PASSOU | viewport declarada |
| regra responsiva | PASSOU | possui @media query |
| estilo embutido | PASSOU | CSS embutido no arquivo |
| comportamento embutido | PASSOU | JavaScript embutido no arquivo |
| integração com armazenamento | FALHOU | não chama /api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records — os dados não persistem |
| campos de entrada | PASSOU | 12 campo(s) |
| marcação balanceada | PASSOU | todas as tags fecham |

**Casos de teste contra os critérios de aceite**

| Caso | Critério | Esperado | Obtido | Resultado | Evidência |
|---|---|---|---|---|---|
| TC-01 | O formulário contém campos para linha, turno (com data pre-preenchida como hoje), resina consumida, tubo bom, refugo de partida, purga de troca, sobra de corte, preço da resina, horas paradas e motivo da parada | Todos os 11 campos obrigatórios presentes e nomeados corretamente | Presentes: linha (linhaField), turno (turnoField), data (dataField), responsável (responsavelField), resina_consumida (resinaBrutoField), tubo_bom (tuboBomField), refugo_partida (refugoPartidaField), purga_troca (purgaTrocaField), sobra_corte (sobraCauteField), preco_resina (precoResinField), horas_paradas (horasParadasField), motivo_parada (motivoParadaField). Total: 12 campos. Data é pré-preenchida via getTodayDate() na função initForm() chamada em DOMContentLoaded | **PASSOU** | IDs dos inputs: id='linhaField', id='turnoField', id='dataField', id='responsavelField', id='resinaBrutoField', id='tuboBomField', id='refugoPartidaField', id='purgaTrocaField', id='sobraCauteField', id='precoResinField', id='horasParadasField', id='motivoParadaField'. Função getTodayDate() retorna data de hoje; chamada em initForm() antes do form ser exibido |
| TC-02 | O campo de linha exibe as 5 linhas de extrusão existentes (dropdown ou buttons) | 5 opções exibidas como buttons ou dropdown com labels Linha 1 até Linha 5 | Implementado como 5 buttons com classe 'linha-button', data-linha de 1 a 5, labels: 'Linha 1', 'Linha 2', 'Linha 3', 'Linha 4', 'Linha 5'. Grid responsivo com auto-fit. | **PASSOU** | HTML: <button type='button' class='linha-button' data-linha='1'>Linha 1</button> até data-linha='5'. CSS: .linha-selector { display: grid; grid-template-columns: repeat(auto-fit, minmax(80px, 1fr)); }. Touch-friendly com min-height: 48px |
| TC-03 | O campo de turno oferece as 2 opções: turno 1, turno 2 | Dropdown com opção padrão vazia e 2 opções: 'Turno 1 (06h - 14h)' e 'Turno 2 (14h - 22h)', values '1' e '2' | <select id='turnoField' name='turno' required> com <option value=''>Selecione um turno</option>, <option value='1'>Turno 1 (06h - 14h)</option>, <option value='2'>Turno 2 (14h - 22h)</option> | **PASSOU** | id='turnoField' contém 3 option tags com values corretos: '', '1', '2' |
| TC-04 | Todos os campos numéricos aceitam apenas números positivos com até 1 casa decimal | Campos type='number' com min='0' step='0.1' e regex no validateNumberField que rejeita 2+ casas decimais | Todos os 7 campos numéricos (resina_consumida, tubo_bom, preco_resina, refugo_partida, purga_troca, sobra_corte, horas_paradas) têm min='0' step='0.1'. Validação: validateNumberField() usa regex /^\d+(\.\d{1})?$/ que aceita inteiros ou máximo 1 decimal. Rejeita negativos via min='0' | **PASSOU** | HTML: input[type='number'] com min='0' step='0.1' em 7 campos. Função validateNumberField(): if (!/^\d+(\d{1})?$/.test(value.trim()) && value.trim() !== '') return erro com mensagem '... deve ter no máximo 1 casa decimal' |
| TC-05 | O responsável é preenchido automaticamente (usuário logado) e exibido read-only | Campo responsavelField preenchido com valor do localStorage 'usuario_logado' ou padrão 'Supervisor', com atributo readonly | input id='responsavelField' name='responsavel' com readonly. Em initForm(): document.getElementById('responsavelField').value = getResponsavel(); onde getResponsavel() retorna localStorage.getItem('usuario_logado') \|\| 'Supervisor' | **PASSOU** | HTML: <input type='text' id='responsavelField' name='responsavel' readonly>. JS: getResponsavel() { return localStorage.getItem('usuario_logado') \|\| 'Supervisor'; } chamada em initForm() |
| TC-06 | O botão Enviar fica desabilitado até todos os campos obrigatórios serem preenchidos | Botão submit inicia desabilitado (disabled), habilita apenas quando linha, turno, e todos 7 campos numéricos + motivo (condicional) estão preenchidos | button id='submitBtn' inicializa com disabled. updateSubmitButtonState() verifica: hasLinha, hasTurno, hasResinaBruto, hasTuboBom, hasPrecoResina, hasRefugoPartida, hasPurgaTroca, hasSobraCorte, hasHorasParadas. Se horas_paradas > 0, também valida hasMotivo. allFilled && trigger = !allFilled atualiza submitBtn.disabled. Chamado em change/input de todos os campos | **PASSOU** | HTML: <button type='submit' id='submitBtn' disabled>. JS: updateSubmitButtonState() { const allFilled = hasLinha && hasTurno && ... && hasMotivo; submitBtn.disabled = !allFilled; }. Event listeners em turnoField, resinaBrutoField, tuboBomField, precoResinField, refugoPartidaField, purgaTrocaField, sobraCauteField, horasParadasField, motivoParadaField |
| TC-07 | Após clicar Enviar, a página redireciona para a tela de resultado | Após validação bem-sucedida, window.location.href aponta para URL de resultado ou redireciona o usuário | CÓDIGO TRUNCADO: O arquivo HTML foi cortado no meio da seção <script>. A função initForm() e o handler de submit do formulário estão incompletos. Não é possível verificar se existe chamada a window.location.href ou redirecionamento | **FALHOU** | Código-fonte fornecido termina abruptamente em 'doc' na linha ~550. O comentário '<!-- ...truncado... -->' confirma que o JavaScript foi cortado antes de conter o handler de submit completo. Sem o handler form submit visible, não é possível validar o redirecionamento obrigatório do critério |
| TC-08 | A aplicação armazena draft de lançamento no localStorage antes de enviar | Antes de fazer POST à API, os dados devem ser gravados em localStorage['turno_drafts'] como array de objetos com id, timestamp_criacao, status='draft' | Função saveDraftToLocalStorage(data) implementada: cria objeto draft com id, timestamp_criacao, status='draft', armazena em localStorage.getItem('turno_drafts'). Também existe função addPendingRecord() que marca status='pending'. Porém, a chamada a saveDraftToLocalStorage() está no código truncado (form submit handler não visível) | **FALHOU** | Função existe: saveDraftToLocalStorage(data) { const draft = { id, ...data, timestamp_criacao, timestamp_tentativa_sync, status: 'draft' }; localStorage.setItem('turno_drafts', JSON.stringify(drafts)); }. MAS o handler form submit não está visível no código fornecido (arquivo truncado). Sem validar se saveDraftToLocalStorage() é chamado no submit, não é possível confirmar uso |
| TC-09 | Se o envio falhar (sem internet ou servidor indisponível), exibe mensagem clara: 'Registro salvo localmente. Será enviado assim que houver conexão.' | Em catch de fetch, showToast() é chamado com mensagem exata ou similar indicando salvamento local | Função showToast(message, type) implementada. Função syncPendingRecords() tem try/catch onde captura erro de sync. Porém, a mensagem exata especificada ('Registro salvo localmente...') não é visível em showToast() nos trechos fornecidos. Apenas showToast() genérica existe. Código de tratamento de erro está truncado | **FALHOU** | showToast() existe mas sem mensagem específica visível: 'Registro salvo localmente. Será enviado assim que houver conexão.' não aparece nos trechos fornecidos do JS. syncPendingRecords() catch { console.error(...) } não mostra showToast com mensagem obrigatória. Arquivo truncado impediu validação completa |
| TC-10 | Quando a conexão volta, a aplicação envia automaticamente todos os registros pendentes | setTimeout ou setInterval monitora navigator.onLine; ao voltar, chama syncPendingRecords() para todos os registros com status pending | setupSyncRetry() implementada: clearInterval anterior, novo setInterval com SYNC_INTERVAL=10000ms que verifica if (navigator.onLine && pendingRecords.length > 0) { syncPendingRecords(); }. Função loadPendingRecordsFromStorage() carrega drafts no startup. Porém, não há listener 'online' ou 'message' do Service Worker que dispare sync imediato quando conexão retorna. Apenas polling a cada 10s | **PASSOU** | setupSyncRetry() { syncRetryInterval = setInterval(() => { if (navigator.onLine && pendingRecords.length > 0) { syncPendingRecords(); } }, SYNC_INTERVAL); }. Chamado em initForm(). loadPendingRecordsFromStorage() executa ao iniciar. Atende parcialmente: usa polling (10s) em vez de event-driven, mas funciona quando online |
| TC-11 | O usuário recebe confirmação visual (toast ou banner) quando a sincronização for bem-sucedida | Após resposta 200 do fetch, showToast('...', 'success') é chamado | Em syncPendingRecords(), após validar response.ok: showToast(`Registro ${record.linha}/${record.turno} sincronizado com sucesso`, 'success'); implementado. Toast será exibido com classe 'success' (border-left-color: var(--ok)) | **PASSOU** | syncPendingRecords() { if (!response.ok) throw; pendingRecords = pendingRecords.filter(...); const drafts = ...; showToast(`Registro ${record.linha}/${record.turno} sincronizado com sucesso`, 'success'); } |
| TC-12 | Registros sincronizados não podem ser duplicados se o usuário clicar várias vezes | Fetch inclui X-Idempotency-Key com UUID único; após sucesso, registro é removido de pendingRecords e localStorage | syncPendingRecords() envia fetch com header: 'X-Idempotency-Key': record.id. Após response.ok, pendingRecords = pendingRecords.filter(r => r.id !== record.id) e localStorage atualizado. Cada draft tem id único gerado por generateUUID(). Uma vez sincronizado, não pode ser enviado novamente pois é removido do array pendingRecords e do localStorage | **PASSOU** | fetch({ method: 'POST', headers: { 'X-Idempotency-Key': record.id }, body: JSON.stringify(...) }); if (!response.ok) throw; pendingRecords = pendingRecords.filter(r => r.id !== record.id); localStorage.setItem('turno_drafts', JSON.stringify(updatedDrafts)); |
| TC-13 | Data é pré-preenchida como hoje | Input data mostra YYYY-MM-DD de hoje ao carregar formulário | getTodayDate() { const today = new Date(); const year = ...; const month = padStart(today.getMonth() + 1); const day = padStart(today.getDate()); return `${year}-${month}-${day}`; }. Em initForm(): document.getElementById('dataField').value = getTodayDate(); | **PASSOU** | getTodayDate() retorna data correta em formato YYYY-MM-DD. Atribuída ao id='dataField' value em initForm() |
| TC-14 | Interface must be obvious for paper-and-pen users (sem treinamento) | Labels em maiúsculas, hints em cinza, 4 seções lógicas (Identificação, Produção, Refugo, Paradas), buttons grandes (48px min-height) | Labels: text-transform: uppercase; color: var(--grafite); margin-bottom: 0.5rem. Section titles agrupam campos por tópico. Hints descrevem cada campo. Buttons: min-height: 48px. Cores de padrão papel/tinta seguem design sem-treinamento | **PASSOU** | CSS: label { text-transform: uppercase; font-weight: 600; }. .field-hint { font-size: 0.8rem; color: var(--grafite); }. Seções: 'Identificação', 'Produção e Consumo', 'Refugo e Perda', 'Paradas'. Button min-height: 48px |
| TC-15 | Página é responsiva e touch-friendly (acesso por celular) | Viewport configurada, @media (max-width: 600px), inputs/buttons com min-height >= 48px, grid adaptativo | <meta name='viewport' content='width=device-width, initial-scale=1.0'> presente. @media (max-width: 600px) { body { padding: 0.75rem; } ... }. Todos os inputs e buttons: min-height: 48px. .linha-selector: grid-template-columns: repeat(auto-fit, minmax(80px, 1fr)) redimensiona em mobile | **PASSOU** | Meta viewport: width=device-width, initial-scale=1.0. @media (max-width: 600px) aplicada. Input/button: min-height: 48px. Grid: repeat(auto-fit, minmax(80px, 1fr)) |
| TC-16 | Campo motivo da parada aparece condicionalmente quando horas_paradas > 0 | Quando horasParadas > 0, motivoParadaGroup recebe classe 'visible'; ao limpar, remove classe e valor | toggleMotivoParagdaField() { const horasParadas = parseFloat(document.getElementById('horasParadasField').value \|\| 0); if (horasParadas > 0) { motivoGroup.classList.add('visible'); } else { motivoGroup.classList.remove('visible'); motivoParadaField.value = ''; } }. CSS: .conditional-field { display: none; } .conditional-field.visible { display: block; }. Disparado em input do horasParadasField | **PASSOU** | id='motivoParadaGroup' class='conditional-field'. toggleMotivoParagdaField() dispara em horasParadasField input event. CSS: .conditional-field { display: none; } .conditional-field.visible { display: block; } |
| TC-17 | Validação: linha vazia retorna erro 'Selecione uma linha' | Erro exibido no linhaError | validateForm() { const linha = formData.get('linha').trim(); if (!linha) { errors.linha = 'Selecione uma linha'; isValid = false; } ... document.getElementById('linhaError').textContent = message; } | **PASSOU** | validateForm() valida linha vazia: if (!linha) { errors.linha = 'Selecione uma linha'; }. Erro exibido em id='linhaError' |
| TC-18 | Validação: campo numérico vazio retorna erro 'é obrigatório' | Erro exibido como '${fieldLabel} é obrigatório' | validateForm() { numericFields.forEach(field => { const value = formData.get(field.name).trim(); if (!value) { errors[field.name] = `${field.label} é obrigatório`; isValid = false; } ... }); } | **PASSOU** | validateForm() itera numericFields e verifica if (!value) { errors[field.name] = `${field.label} é obrigatório`; } |
| TC-19 | Validação: campo numérico com 2+ casas decimais retorna erro | Erro: '${fieldLabel} deve ter no máximo 1 casa decimal' | validateNumberField(value, fieldName) { ... if (!/^\d+(\.\d{1})?$/.test(value.trim()) && value.trim() !== '') { return `${fieldName} deve ter no máximo 1 casa decimal`; } ... }. Regex rejeita '10.25', aceita '10.2' e '10' | **PASSOU** | validateNumberField() regex: /^\d+(\.\d{1})?$/ aceita inteiro ou máximo 1 decimal. '10.25' falha, '10.2' passa |
| TC-20 | Caso negativo: motivo parada obrigatório se horas_paradas > 0 | validateForm() retorna isValid=false; erro exibido em motivoParadaError | validateForm() { const horasParadas = parseFloat(formData.get('horas_paradas') \|\| 0); if (horasParadas > 0) { const motivo = formData.get('motivo_parada').trim(); if (!motivo) { errors.motivo_parada = 'Motivo da parada é obrigatório quando há paradas'; isValid = false; } } } | **PASSOU** | validateForm() condicional: if (horasParadas > 0) { if (!motivo) { errors.motivo_parada = '...obrigatório quando há paradas'; }} |
| TC-21 | Validação automática falhou: integração com /api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records não funciona | POST enviado para /api/apps/.../records e dados persistem | syncPendingRecords() usa fetch(`${API_BASE}/records`, { method: 'POST', ... }) onde API_BASE='/api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2'. PORÉM: (1) O código HTML está truncado; o handler do form submit não está visível; (2) saveDraftToLocalStorage() existe mas sua chamada no submit não foi validada; (3) Não há confirmação de que o POST é realmente disparado após envio do formulário | **FALHOU** | API_BASE definido corretamente, mas código truncado impediu validar: (a) Se form submit dispara saveDraftToLocalStorage() antes do fetch; (b) Se fetch é executado ou se há tratamento de erro offline. Validação automática '[FALHOU] integração com armazenamento: não chama /api/...' confirma falha prática |

**Pendências**

- 1. Completar o arquivo HTML: fornecer o JavaScript completo do form submit handler, incluindo saveDraftToLocalStorage(), validação e redirecionamento para tela de resultado (window.location.href ou similar)
- 2. Implementar tratamento offline obrigatório: no catch do fetch, disparar showToast('Registro salvo localmente. Será enviado assim que houver conexão.', 'info') ANTES de salvar no localStorage
- 3. Validar chamada à API: verificar se endpoint '/api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records' está ativo e responde com HTTP 200; se estiver retornando erro, corrigir rota ou configuração no backend
- 4. Garantir que form submit dispara: (a) saveDraftToLocalStorage(formData); (b) ativa syncPendingRecords(); (c) se sucesso, redireciona para /resultado ou tela de confirmação
- 5. Testar fluxo offline completo em celular com DevTools (desabilitar internet durante submit, verificar toast e localStorage, reconectar e validar sincronização automática após 10s)

## Resultado da Perda — REPROVADO

A tela implementa corretamente os cálculos, formatação, status visual, composição, aviso de diferença e gráfico. Porém, o código do botão Confirmar está TRUNCADO (falta JSON.stringify e tratamento de resposta), impedindo a gravação de registros. Além disso, a verificação automática falhou em 'campos de entrada', indicando problema estrutural na entrada de dados.


**Verificações automáticas sobre o arquivo**

| Verificação | Resultado | Detalhe |
|---|---|---|
| documento completo | PASSOU | arquivo começa com <!doctype html> |
| fechamento do html | PASSOU | </html> presente |
| tamanho plausível | PASSOU | 24793 caracteres |
| sem recursos externos | PASSOU | nenhum recurso remoto — a página abre sem internet |
| compatível com iframe | PASSOU | sem alert/confirm/prompt/window.parent |
| meta viewport | PASSOU | viewport declarada |
| regra responsiva | PASSOU | possui @media query |
| estilo embutido | PASSOU | CSS embutido no arquivo |
| comportamento embutido | PASSOU | JavaScript embutido no arquivo |
| campos de entrada | FALHOU | 1 campo(s) |
| cálculo no cliente | PASSOU | recalcula por evento de interface |
| marcação balanceada | PASSOU | todas as tags fecham |

**Casos de teste contra os critérios de aceite**

| Caso | Critério | Esperado | Obtido | Resultado | Evidência |
|---|---|---|---|---|---|
| TC-01 | O resultado exibe perda total em kg (com 2 casas decimais) | Perda total em kg com 2 casas decimais (ex: 12,34 kg) | Código linha ~350: document.getElementById('metricaPerdaTotal').textContent = perdaTotal.toFixed(2) + ' kg'; — Sim, implementado | **PASSOU** | ID #metricaPerdaTotal + .toFixed(2) + ' kg' |
| TC-02 | O resultado exibe perda percentual (com 1 casa decimal e símbolo %) | Perda percentual com 1 casa decimal e símbolo % (ex: 5,2%) | Código linha ~351: document.getElementById('metricaPerdaPerc').textContent = perdaPercentual.toFixed(1) + '%'; — Sim, implementado | **PASSOU** | ID #metricaPerdaPerc + .toFixed(1) + '%' |
| TC-03 | O resultado exibe custo da perda em R$ (com 2 casas decimais) | Custo da perda em reais com 2 casas decimais (ex: R$ 123,45) | Código linha ~352: document.getElementById('metricaCustoPer').textContent = 'R$ ' + custoPer.toFixed(2); — Sim, implementado | **PASSOU** | ID #metricaCustoPer + 'R$ ' + .toFixed(2) |
| TC-04 | O resultado mostra o status da meta: OK (verde, perda < 4%), Atenção (amarelo, 4% ≤ perda ≤ 7%), Crítico (vermelho, perda > 7%) | Status visual com cor: verde (OK <4%), amarelo (Atenção 4-7%), vermelho (Crítico >7%) | Código linhas ~354-368: Implementa if (perdaPercentual < 4) { cor='ok' }, else if (perdaPercentual <= 7) { cor='atencao' }, else { cor='critico' }. Classes CSS .semaforo-luz.ok (#2C6E49), .atencao (#9A5407), .critico (#A32116) aplicadas ao elemento #semaforoLuz | **PASSOU** | ID #semaforoLuz com classes .ok/.atencao/.critico + CSS cores #2C6E49 (verde), #9A5407 (amarelo), #A32116 (vermelho) |
| TC-05 | A tela exibe um resumo dos dados lançados (linha, turno, resina, tubo bom, etc) para validação visual | Resumo completo com linha, turno, tipo de resina, resina consumida, tubo bom, preço, horas paradas, motivo da parada | Código linhas ~336-344: Todos os 8 campos de resumo são populados (textContent atribuído). Cada um exibe dados do objeto dadosTurno | **PASSOU** | IDs #resumoLinha, #resumoTurno, #resumoResina, #resumoResinaCons, #resumoTuboBom, #resumoPreco, #resumoHorasParradas, #resumoMotivoParada preenchidos via sessionStorage |
| TC-06 | A tela exibe a composição da perda: refugo de partida, purga de troca, sobra de corte em kg e % do total | Tabela com 3 componentes, cada um com kg (2 casas decimais) e % (1 casa decimal) | Código linhas ~374-381: Refugo (compRefugoKg.toFixed(2), compRefugoPerc.toFixed(1)+'%'), Purga (compPurgaKg.toFixed(2), compPurgaPerc.toFixed(1)+'%'), Sobra (compSobraKg.toFixed(2), compSobraPerc.toFixed(1)+'%'). Valores calculados corretamente | **PASSOU** | IDs #compRefugoKg, #compRefugoPerc, #compPurgaKg, #compPurgaPerc, #compSobraKg, #compSobraPerc + .toFixed(2) e .toFixed(1) |
| TC-07 | A tela exibe a diferença não explicada em kg (perda total - refugo total) | Diferença não explicada em kg com 2 casas decimais | Código linha ~345: const diferenca = perdaTotal - refugoTotal; Linha ~383: document.getElementById('compDifKg').textContent = diferenca.toFixed(2); — Implementado corretamente | **PASSOU** | ID #compDifKg + cálculo (perdaTotal - refugoTotal).toFixed(2) |
| TC-08 | Todos os cálculos ocorrem sem recarregar a página | Cálculos executados em JavaScript puro, sem POST/recarreg de página | Código linhas ~313-320: carregarDados() lê sessionStorage e chama calcularResultados(). Nenhum window.reload() ou location.href encontrado no escopo dos cálculos. Cálculos ocorrem em client-side puro | **PASSOU** | Função calcularResultados() executada via addEventListener, sessionStorage.getItem(), sem reload |
| TC-09 | Existe um botão Confirmar Registro que grava tudo em registros_turno com timestamp | Botão executa POST com timestamp_criacao (ISO format) gravando em registros_turno | Código linhas ~449-465: botão #btnConfirmar possui addEventListener('click', async). Código adiciona dadosTurno.timestamp_criacao = new Date().toISOString() (linha ~457). Fetch POST para '/api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records' executado. MAS: código está TRUNCADO em linha ~464 (body: JSON.strin...) — implementação incompleta | **FALHOU** | Botão #btnConfirmar + addEventListener + fetch POST; porém código truncado em 'JSON.strin' — impossível verificar se gravação completa funciona |
| TC-10 | Se a diferença for > 5% da perda total, um aviso aparece: 'Diferença não explicada detectada. Revisar pesagem?' | Aviso exibido quando diferença absoluta > 5% da perda total | Código linhas ~385-391: if (Math.abs(diferenca) > (perdaTotal * 0.05)) { avisoValidacao.textContent = 'Diferença não explicada detectada. Revisar pesagem?'; classList.add('ativo'); } — Implementado corretamente | **PASSOU** | Condicional Math.abs(diferenca) > (perdaTotal * 0.05) + #avisoValidacao.classList.add('ativo') |
| TC-11 | A tela exibe um pequeno gráfico em pizza mostrando composição: refugo %, purga %, sobra %, diferença % | Canvas com gráfico de pizza mostrando proporções de refugo, purga, sobra e diferença | Código linhas ~410-475: Função desenharComposicao(dados) implementa canvas 2D com ctx.arc(), loop sobre componentes, cores diferenciadas, e legenda em texto (linhas ~463-475). Chamada em linha ~395 | **PASSOU** | Função desenharComposicao() + canvas #graficoComposicao + ctx.arc() + legenda com cores #2C6E49, #1F4E5F, #9A5407, #A32116 |
| TC-12 | O supervisor pode inserir um campo de observação livre ao confirmar (até 200 caracteres) para explicar diferenças | Campo textarea livre até 200 caracteres, com contador dinâmico | Código linhas ~139-153 (HTML) + linhas ~442-448 (JS): textarea #observacao com placeholder, listener 'input' que trunca a len > 200 (substring(0,200)), contador #observacaoContador atualizado. Está condicional (display: none, ativo só se diferença > 5%) | **PASSOU** | textarea #observacao + addEventListener('input') + e.target.value.substring(0, 200) + #observacaoContador |
| TC-13 | Caso negativo: lista sem registros (estado vazio) deve mostrar mensagem e desabilitar confirmar | Mensagem 'Nenhum dado de turno carregado' visível, botão Confirmar desabilitado | Código linhas ~477-481: mostrarEstadoVazio() ativa #estadoVazio.classList.add('ativo'), desabilita #btnConfirmar.disabled = true. Função chamada em linha ~317 se !dados. HTML linhas ~220-224 contém mensagem correta | **PASSOU** | #estadoVazio.classList.add('ativo') + #btnConfirmar.disabled = true + mensagem 'Nenhum dado de turno carregado' |
| TC-14 | Caso negativo: resina consumida inválida (<= 0) deve mostrar aviso e estado vazio | Aviso exibido, estado vazio ativado, cálculos bloqueados | Código linhas ~322-327: if (!d.resina_consumida_kg \|\| d.resina_consumida_kg <= 0) { mostrarAviso('Erro: resina consumida inválida...'); mostrarEstadoVazio(); return; } — Implementado | **PASSOU** | Condicional !d.resina_consumida_kg \|\| d.resina_consumida_kg <= 0 + mostrarAviso() + mostrarEstadoVazio() |
| TC-15 | Verificação automática: campo de entrada falhou (1 campo missing) | Todos os campos necessários devem estar presentes e acessíveis | Verificação automática reportou falha. Análise manual: existem apenas 2 inputs visíveis no HTML (botões, textarea). O campo 'observacao' está condicional (display: none). Não há campo obrigatório de entrada para dados iniciais (tela apenas EXIBE cálculos, não coleta dados iniciais). Possível que a automação esperasse campo de entrada de dados primários (linha, resina, etc) que NÃO existem nesta tela (são coletados na tela anterior via sessionStorage) | **FALHOU** | Relatório automático [FALHOU] campos de entrada: 1 campo(s) — campo específico não identificado no relatório, mas falha confirmada |
| TC-16 | Integração completa: botão Confirmar Registro grava com sucesso | Toast sucesso exibido, dados gravados em registros_turno | Código linhas ~449-465: Fetch iniciado, MAS CÓDIGO TRUNCADO em 'body: JSON.strin' — impossível verificar: (a) se JSON.stringify está completo, (b) se o tratamento de resposta (then/catch) está implementado, (c) se toast de sucesso é exibido, (d) se dados são realmente persistidos | **FALHOU** | Código truncado em linha ~464: 'body: JSON.strin...' — falta implementação de resposta, tratamento de erro e confirmação visual |

**Pendências**

- Completar implementação do botão Confirmar Registro: finalizar JSON.stringify(dadosTurno) no body do fetch POST
- Implementar tratamento de resposta do fetch: .then(resp => resp.json()).then(data => { mostrarToast('Registro gravado com sucesso!', 'sucesso'); ... }).catch(err => { mostrarToast('Erro ao gravar: ' + err.message, 'erro'); })
- Investigar e corrigir falha de 'campos de entrada' reportada pela automação: adicionar campo de entrada obrigatório ou identificar qual campo está faltando
- Validar se campo textarea #observacao deve estar condicional (display: none) ou sempre visível — ST-06 não especifica que deve aparecer só em diferenças > 5%
- Testar gravação real em registros_turno com dados de turno completos para confirmar persistência

## Comparativo por Linha e Turno — REPROVADO

A entrega contém falhas críticas: (1) a filtragem por período não é implementada na tabela — os filtros são coletados mas ignorados na renderização; (2) a função de gráfico foi truncada no código-fonte, impossibilitando validação; (3) o Dev coletou todos os dados de período mas não aplica o filtro selecionado. O critério de aceite exige filtragem funcional e o código não entrega isso.


**Verificações automáticas sobre o arquivo**

| Verificação | Resultado | Detalhe |
|---|---|---|
| documento completo | PASSOU | arquivo começa com <!doctype html> |
| fechamento do html | PASSOU | </html> presente |
| tamanho plausível | PASSOU | 26732 caracteres |
| sem recursos externos | PASSOU | nenhum recurso remoto — a página abre sem internet |
| compatível com iframe | PASSOU | sem alert/confirm/prompt/window.parent |
| meta viewport | PASSOU | viewport declarada |
| regra responsiva | PASSOU | possui @media query |
| estilo embutido | PASSOU | CSS embutido no arquivo |
| comportamento embutido | PASSOU | JavaScript embutido no arquivo |
| integração com armazenamento | PASSOU | usa /api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records |
| marcação balanceada | PASSOU | todas as tags fecham |

**Casos de teste contra os critérios de aceite**

| Caso | Critério | Esperado | Obtido | Resultado | Evidência |
|---|---|---|---|---|---|
| TC-01 | O painel exibe uma tabela com colunas: Linha, Turno, Períodos (últimos 7 dias, últimos 30 dias, mês atual) | Tabela contendo colunas: LINHA, TURNO, PERDA % (7D), PERDA % (30D), PERDA % (MÊS) | Código gera tabela com exatamente essas colunas no <thead> (linhas ~465-475). HTML contém: <th>LINHA</th>, <th>TURNO</th>, <th class="sortavel asc" data-sort="7d">PERDA % (7D)</th>, <th class="sortavel" data-sort="30d">PERDA % (30D)</th>, <th class="sortavel" data-sort="mes">PERDA % (MÊS)</th> | **PASSOU** | renderizarTabela() linhas 465-475; HTML gerado inclui todos os cabeçalhos de período |
| TC-02 | Para cada célula (linha/turno/período), exibe perda percentual média e número de registros | Cada célula de período mostra valor percentual formatado com 2 casas decimais e quantidade de registros abaixo | Código renderiza cada célula com classe detalhes-celula contendo span.valor-principal (${perda7d}%) e span.valor-secundario (${reg7d} reg.). Exemplo linhas 480-484: <span class="valor-principal">${perda7d}%</span><span class="valor-secundario">${reg7d} reg.</span> | **PASSOU** | renderizarTabela() linhas 476-489; estrutura HTML com .detalhes-celula contém valor-principal e valor-secundario para cada célula |
| TC-03 | A tabela é ordenável por perda percentual (descendente por padrão) | Tabela inicia com ordem descendente. Cabeçalhos ordenáveis possuem event listener click. Função ordenarTabela() alterna asc/desc | Linhas 489-510: filtrados.sort() ordena por perda30d descendente (perdaB - perdaA). Classe inicial 'asc' em primeira coluna (linha 470). Event listener adicionado em linhas 528-532. Função ordenarTabela() implementada em linhas 546-572, alternando asc/desc | **PASSOU** | Função ordenarTabela() linhas 546-572; sort descendente padrão linhas 489-493; listener click em th.sortavel linhas 528-532 |
| TC-04 | Linhas/turnos com perda > 7% (crítico) aparecem destacadas em vermelho | Células com perda > 7 recebem classe 'celula-critica' com fundo #fce4e4 (vermelho claro) e texto var(--critico) #A32116 | Função obterClasse() linhas 540-544 retorna 'celula-critica' quando valor > 7. CSS linhas 268-271 define .celula-critica { background-color: #fce4e4; color: var(--critico); font-weight: 600; }. Aplicado nas células linhas 482, 486, 490 via classe dinâmica | **PASSOU** | obterClasse() linha 541; CSS .celula-critica linhas 268-271; aplicação dinâmica linha 482 (classe7d = obterClasse(perda7d)) |
| TC-05 | Linhas/turnos com perda 4-7% (atenção) aparecem em amarelo | Células com 4 ≤ perda ≤ 7 recebem classe 'celula-atencao' com fundo #fff8e1 (amarelo) e texto var(--alerta) #9A5407 | Função obterClasse() linha 542 retorna 'celula-atencao' quando valor >= 4 (após verificar > 7). CSS linhas 273-276 define .celula-atencao { background-color: #fff8e1; color: var(--alerta); font-weight: 600; }. Aplicado nas células via mesma lógica | **PASSOU** | obterClasse() linha 542; CSS .celula-atencao linhas 273-276 |
| TC-06 | Existe um gráfico de barras ou linha mostrando tendência de perda de uma linha específica ao longo dos últimos 30 dias | Gráfico renderizado em <canvas id="chart-tendencia">. Dados agrupados por dia nos últimos 30 dias. Função criarGraficoLinha() implementada | Canvas presente linhas 125-128. Função renderizarGrafico() linhas 574-606 processa tendencia30 agrupando por dia. Função criarGraficoLinha() iniciada linha 608 (código truncado). Estado.tendencia30 preenchido na agregação linhas 451-453 | **FALHOU** | Código-fonte foi truncado no final (---truncado---) e a implementação completa de criarGraficoLinha() não é visível. Não é possível verificar se o gráfico realmente renderiza de forma funcional. O Canvas existe mas a função está incompleta |
| TC-07 | O usuário pode filtrar por linha (dropdown com as 5 linhas ou 'Todas') | Select com opção 'Todas' (value='') e 5 linhas (L1-L5). Filtragem aplicada ao clicar em 'Aplicar' | Select presente linhas 59-66 com <option value="">Todas</option> e opções L1-L5. Filtragem implementada em aplicarFiltros() linha 389 (state.filtroLinha = elementos.filtroLinha.value) e renderizarTabela() linhas 503-506 filtrando dados.filter(d => d.linha === state.filtroLinha) | **PASSOU** | Select#filtro-linha linhas 59-66; filtragem em renderizarTabela() linhas 503-506 |
| TC-08 | O usuário pode filtrar por período (últimos 7 dias, 30 dias, mês atual, custom range) | Select com 4 opções. Seleção 'custom' exibe campos de data. Validação de data inicio < fim e intervalo <= 365 dias | Select presente linhas 68-73 com 4 opções (7d, 30d, mes_atual, custom). Listener em linha 365 mostra/esconde #filtro-custom. Validação em aplicarFiltros() linhas 395-406 verifica inicio/fim preenchidas, inicio < fim, e intervalo <= 365 dias. Porém: a filtragem real por período NÃO é aplicada na tabela. Os dados agregados contêm periodos 7d/30d/mes_atual mas a filtragem por state.filtroPeriodo não existe em renderizarTabela() | **FALHOU** | Select e validação OK (linhas 68-73, 395-406) MAS renderizarTabela() não filtra dados por state.filtroPeriodo. Tabela sempre mostra todos os registros independente da seleção de período |
| TC-09 | O painel carrega dados de registros_turno agrupados e média | Fetch em '/api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records?tipo=registros_turno'. Agregação por linha\|turno. Média calculada para cada período | Fetch linhas 373-375: url = '/api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records?tipo=registros_turno'. Função agregarPorLinhaeTurno() linhas 410-468 agrupa por key `${linha}\|${turno}` (linha 437) e calcula média linhas 457-462: dados.media = dados.valores.reduce(...) / valores.length | **PASSOU** | Fetch linha 374; agregação linha 437 (key = linha\|turno); média calculada linhas 457-462 |
| TC-10 | Caso negativo: painel deve exibir mensagem quando lista vazia (nenhum registro ou nenhum após filtro) | Mensagem amigável no .estado-vazio explicando a situação | Duas verificações em renderizarTabela(): linhas 502-508 se dados vazio, e linhas 514-518 se filtrados vazio após filtro de linha. Ambas exibem div.estado-vazio com mensagem descritiva | **PASSOU** | renderizarTabela() linhas 502-508 e 514-518; .estado-vazio (CSS linhas 226-232) |
| TC-11 | Caso negativo/borda: período customizado deve validar datas vazias e intervalo inválido | Mensagens de erro específicas: 'Informe ambas as datas' se vazio, 'Data inicial deve ser menor' se inicio >= fim, 'Intervalo deve estar nos últimos 365 dias' se fora do intervalo | Validações em aplicarFiltros() linhas 395-406: verifica dataInicio && dataFim (linha 397), compara inicio >= fim (linha 403), verifica limites de 365 dias (linha 411). Mensagens de erro exibidas via exibirMensagem() | **PASSOU** | aplicarFiltros() linhas 397, 403, 411 com mensagens correspondentes |
| TC-12 | Restrição: atacar primeiro onde dói mais — visualização deve destacar o crítico (> 7%) | Tabela inicia com maiores perdas no topo. Cores críticas (#fce4e4 vermelho + font-weight 600) são bem destacadas | Ordenação descrescente padrão linhas 489-493 (perdaB - perdaA). CSS crítico linhas 268-271 com background #fce4e4 e font-weight 600. Coluna (30d) ordenada descendente por padrão | **PASSOU** | Sort descrescente linhas 489-493; CSS .celula-critica linhas 268-271 com destaque visual |
| TC-13 | Caso negativo: filtro de linha com seleção de linha específica mas sem registros deve exibir mensagem | Mensagem 'Nenhum registro encontrado para a linha selecionada' | Verificação em renderizarTabela() linhas 514-518: se filtrados.length === 0 após state.filtroLinha, exibe estado-vazio com mensagem 'Nenhum registro encontrado para a linha selecionada' | **PASSOU** | renderizarTabela() linhas 514-518 |
| TC-14 | Erro crítico: filtragem por período não está implementada na renderização de tabela | renderizarTabela() deve usar state.filtroPeriodo para selecionar qual coluna exibir ou qual período agregar | state.filtroPeriodo é definido e validado em aplicarFiltros() mas NUNCA é utilizado em renderizarTabela(). A tabela sempre renderiza as 3 colunas (7d, 30d, mes_atual) independentemente. Não há filtragem de período na tabela | **FALHOU** | state.filtroPeriodo definido linha 388 mas não aparece em renderizarTabela() (linhas 496-519). Critério exige filtragem funcional por período, não apenas validação |

**Pendências**

- Implementar lógica de filtragem por período em renderizarTabela(): usar state.filtroPeriodo para selecionar quais colunas de período exibir e qual período usar para ordenação padrão
- Completar e validar a função criarGraficoLinha() — código foi truncado e não é possível verificar se renderiza corretamente
- Testar fluxo completo: aplicar filtro período 'Últimos 7 dias' deve exibir APENAS coluna (7D) ou alternar a ordenação padrão para a coluna 7d, não as 3 colunas ao mesmo tempo
- Validar agregação de dados customizados quando period='custom' com datas específicas — código atual não filtra registros por dataInicio/dataFim antes de agregar

## Consulta de Registros — REPROVADO

O código entrega a maioria dos critérios de aceite, porém há uma falha crítica: não existe validação de datas contraditórias (data final anterior à inicial), deixando o usuário sem feedback claro. Isso prejudica a usabilidade e a auditoria. O comportamento é silencioso — simplesmente retorna zero registros.


**Verificações automáticas sobre o arquivo**

| Verificação | Resultado | Detalhe |
|---|---|---|
| documento completo | PASSOU | arquivo começa com <!doctype html> |
| fechamento do html | PASSOU | </html> presente |
| tamanho plausível | PASSOU | 26524 caracteres |
| sem recursos externos | PASSOU | nenhum recurso remoto — a página abre sem internet |
| compatível com iframe | PASSOU | sem alert/confirm/prompt/window.parent |
| meta viewport | PASSOU | viewport declarada |
| regra responsiva | PASSOU | possui @media query |
| estilo embutido | PASSOU | CSS embutido no arquivo |
| comportamento embutido | PASSOU | JavaScript embutido no arquivo |
| integração com armazenamento | PASSOU | usa /api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records |
| marcação balanceada | PASSOU | todas as tags fecham |

**Casos de teste contra os critérios de aceite**

| Caso | Critério | Esperado | Obtido | Resultado | Evidência |
|---|---|---|---|---|---|
| TC-01 | A tela exibe um formulário de busca com filtros: linha, turno, data inicial, data final, motivo da parada (texto livre) | Existem 5 campos de filtro: linha (text), turno (select), data inicial (date), data final (date), motivo (text) | Presentes: <input id='filtroLinha' type='text'>, <select id='filtroTurno'>, <input id='filtroDataInicial' type='date'>, <input id='filtroDataFinal' type='date'>, <input id='filtroMotivo' type='text'> | **PASSOU** | IDs: filtroLinha (linha 261), filtroTurno (linha 266), filtroDataInicial (linha 271), filtroDataFinal (linha 275), filtroMotivo (linha 280) |
| TC-02 | O resultado exibe uma tabela com as 10 colunas especificadas: Data, Linha, Turno, Resina Consumida, Tubo Bom, Perda Total, Perda %, Custo, Responsável, Motivo da Parada | Tabela com exatamente 10 colunas nos cabeçalhos e dados preenchidos corretamente | Tabela contém 10 <th>: Data, Linha, Turno, Resina Consumida, Tubo Bom, Perda Total, Perda %, Custo, Responsável, Motivo da Parada (linhas 299-309). Renderização em renderizarTabela() popula as mesmas 10 colunas (linhas ~735-746) | **PASSOU** | HTML <table><thead><tr> com 10 <th> elementos (linhas 298-310); renderizarTabela() gera <td> para cada coluna em sequência correta |
| TC-03 | A tabela é paginada (máx 20 registros por página) | Variável registrosPorPagina = 20 e slicing dos registros respeitando este limite | const registrosPorPagina = 20 (linha 620); função buscarRegistros() calcula 'const inicio = (paginaAtual - 1) * registrosPorPagina; const fim = inicio + registrosPorPagina; const registrosPagina = registros.slice(inicio, fim)' (linhas ~769-771) | **PASSOU** | Constante registrosPorPagina = 20 (linha 620); slice aplicado em buscarRegistros (linhas 769-771) e paginaAnterior/proximaPagina (linhas ~795-816) |
| TC-04 | Cada linha é clicável e abre um painel de detalhes com todos os campos do registro e timestamp de criação | Cada <tr> gerada tem listener click que chama abrirDrawer; drawer exibe todos os campos + timestamp | Em renderizarTabela(): 'tr.addEventListener('click', () => abrirDrawer(registro))' (linha ~742); abrirDrawer() popula 16 campos incluindo detalheTimestamp via formatarDataHora(registro.criado_em) (linha ~690) | **PASSOU** | addEventListener no contexto da renderização (linha 742); detalheTimestamp preenchido com formatarDataHora(registro.criado_em) (linha 690); drawer-conteudo contém todos os campos (linhas 331-383) |
| TC-05 | O painel de detalhes exibe imutavelmente: quem lançou (responsável), quando foi lançado (data/hora), qual foi o turno | Responsável, timestamp (data/hora), turno devem estar sempre visíveis e preenchidos sem inputs de edição | Drawer exibe: detalheResponsavel (linha 332) = registro.dados.responsavel; detalheTimestamp (linha 334) = formatarDataHora(registro.criado_em); detalheTurno (linha 341) = registro.dados.turno. Todos são <div> somente leitura (não inputs) | **PASSOU** | IDs: detalheResponsavel (linha 332), detalheTimestamp (linha 334), detalheTurno (linha 341) — preenchidos em abrirDrawer linhas 688-692, sem inputs editáveis |
| TC-06 | Existe um botão Exportar (CSV) que baixa os registros filtrados | Botão com id botaoExportar e função que gera CSV com registros de ultimosResultados | Botão presente: <button id='botaoExportar' class='botao-secundario'>Exportar CSV</button> (linha 286); função exportarCSV() existe (linha ~820+) com lógica de construção de CSV e download via blob | **PASSOU** | ID botaoExportar (linha 286); função exportarCSV em linhas ~820-880 com geração de CSV, verificação de ultimosResultados.length, e download via URL.createObjectURL |
| TC-07 | Todos os dados carregam de registros_turno (endpoint correto com tipo=registros_turno) | fetch aponta para /api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records?tipo=registros_turno | const response = await fetch('/api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records?tipo=registros_turno') (linha ~745) | **PASSOU** | Fetch na linha ~745 com URL exato: '/api/apps/empresa-tubos-meridiano-extrus-o-de-tu-2/records?tipo=registros_turno' |
| TC-08 | Responsável e timestamp devem ser guardados e exibidos (restrição de auditabilidade) | Responsável preenchido em cada registro.dados.responsavel; timestamp em registro.criado_em; ambos exibidos no drawer e na tabela | Tabela exibe responsável em coluna 'Responsável' (linha ~742 do renderizarTabela); drawer exibe detalheResponsavel com registro.dados.responsavel (linha 688) e detalheTimestamp com formatarDataHora(registro.criado_em) (linha 690). Dados vêm do JSON via fetch | **PASSOU** | renderizarTabela linha ~742: '<td>${registro.dados.responsavel \|\| '—'}</td>'; abrirDrawer linhas 688 e 690 populam detalheResponsavel e detalheTimestamp |
| TC-09 | Caso negativo: comportamento quando lista está vazia (nenhum registro encontrado) | Função exibirMensagemVazia() é chamada e exibe mensagem 'Nenhum registro encontrado' | renderizarTabela() verifica 'if (registros.length === 0) { exibirMensagemVazia(); return; }' (linha ~720); exibirMensagemVazia() existe (linha ~677) e cria div.mensagem-vazio com título 'Nenhum registro encontrado' | **PASSOU** | Verificação em renderizarTabela linha ~720; função exibirMensagemVazia linhas ~677-684 com classe mensagem-vazio |
| TC-10 | Caso negativo: comportamento com filtros inválidos ou data final anterior à inicial | Filtro de data funciona com comparação string (YYYY-MM-DD) ou há validação; erros de rede são capturados e exibem mensagem de erro | Filtro por dataFinal usa comparação string: 'if (filtros.dataFinal && reg.dados.data > filtros.dataFinal)' (linha ~761); não há validação explícita de data > data anterior. Erros de rede são capturados no catch (linha ~777): 'exibirErro(erro.message)' | **FALHOU** | Linha ~761 compara datas como strings (YYYY-MM-DD format comparável), mas não valida se dataFinal < dataInicial. Não há feedback para o usuário se a data final for menor que a inicial — simplesmente retorna zero registros sem aviso. |
| TC-11 | Caso negativo: exportar CSV quando nenhum registro foi buscado | Mensagem de erro: 'Nenhum registro para exportar. Execute uma busca primeiro.' | exportarCSV() verifica: 'if (ultimosResultados.length === 0) { exibirErro('Nenhum registro para exportar. Execute uma busca primeiro.'); return; }' (linhas ~821-824) | **PASSOU** | exportarCSV linhas ~821-824 com validação explícita de ultimosResultados.length |
| TC-12 | Responsável é exibido imutavelmente no painel de detalhes (sem possibilidade de edição) | detalheResponsavel e detalheTimestamp são <div class='valor'>, não inputs | drawer-conteudo usa apenas <div class='valor'> para exibição (linhas 332-384); nenhum <input> para edição está presente | **PASSOU** | Elementos com id detalheResponsavel, detalheTimestamp, etc. são <div id='...' class='valor'>, não inputs (linhas 332-384) |

**Pendências**

- Adicionar validação no formulário: quando o usuário clica em 'Buscar', validar se filtroDataFinal >= filtroDataInicial. Se inválido, exibir exibirErro('Data final não pode ser anterior à data inicial.') antes de chamar fetch.
- Implementar esta validação dentro de buscarRegistros(), logo após capturar os valores dos filtros (após linha ~755, antes do fetch).
